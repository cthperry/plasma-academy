# Plasma Academy P0

零框架、純靜態的 Plasma Academy P0 骨架。此版本依 `docs/11-build-roadmap.md` 的 P0 範圍建立網站骨架、設計系統、核心 JS、搜尋索引、進度追蹤與互動元件共用庫雛形。

## 指令

```powershell
npm run build
npm run check
npm run serve
```

產物位於 `dist/client`。Sites 部署時使用 `worker/index.js` 作為靜態資源伺服入口。
