# 11 — 實作路線圖

## 分期總覽

| Phase | 內容 | 產出 | 相依 |
|---|---|---|---|
| **P0** | 網站骨架與設計系統 | 可跑的空站 + 章節模板 + 導覽 + 進度追蹤 + 共用元件庫 | — |
| **P1** | L1 全部內容 + A01–A07 | 8 小時課程可完整學習 | P0 |
| **P2** | L2 內容 + 氣體百科 + A08–A16 | 16 小時課程 + 可用的氣體工具 | P1 |
| **P3** | L3 內容 + 缺陷圖鑑 + A17–A25 | 20 小時課程 + 可用的診斷工具 | P2 |
| **P4** | L4 內容 + A26–A32 + 測驗中心 | 全站完成 + 認證 | P3 |

**每一期結束都是一個可交付、可用的產品**,而不是半成品。P1 結束後就可以先給新人用 L1。

---

## P0 — 骨架與設計系統

### 為什麼先做這個

33 個互動元件如果各自造輪子,程式碼會膨脹三倍且無法維護。P0 的核心任務是把**共用模組**先立起來。

### 工作項目

**站點建置**
- [x] Node 建置腳本:模板 + 內容片段 → 完整靜態 HTML(不引入框架)
- [x] 目錄結構與 URL 規劃(見 `06`)
- [x] 搜尋索引產生器(建置時掃描內容產生倒排索引 JSON)

**設計系統**
- [x] `base.css` — token(深淺兩套)、reset、排版
- [x] `layout.css` — 三欄版面、響應式斷點、導覽
- [x] `components.css` — 公式卡、五種提示方塊、表格、tooltip、互動元件容器
- [x] `print.css` — 列印樣式
- [x] 對比驗證腳本(自動檢查所有 token 組合的對比度)

**核心 JS**
- [x] `theme.js` — 三態主題切換,`<head>` 內同步執行防閃爍,派發 `pa:themechange`
- [x] `progress.js` — localStorage 進度追蹤、匯出匯入
- [x] `nav.js` — 側欄、麵包屑、鍵盤導覽、章節間跳轉
- [x] `tooltip.js` — 術語 hover/focus 提示
- [x] `search.js` — 前端搜尋(延遲載入索引)
- [x] `units.js` — 常駐單位換算工具列

**互動元件共用庫**(最關鍵的一項)
- [x] `lifecycle.js` — `init/start/stop/reset/destroy` 契約 + `IntersectionObserver` 自動停止
- [x] `controls.js` — 滑桿、分段控制、開關、播放鈕、數值面板(統一樣式與鍵盤支援)
- [x] `plot.js` — SVG 座標軸、曲線、對數軸、游標讀值、圖例
- [x] `particle-engine.js` — 粒子系統(位置、速度、碰撞、繪製、主題適配)
- [x] `canvas-theme.js` — 從 CSS token 讀色彩,監聽主題變更重繪
- [x] `plasma-model.js` — 物理常數、0-D 模型、公用計算式(λ_D、u_B、λ、τ、Child-Langmuir)

**資料模組骨架**
- [x] `data/curriculum.js` — 25 模組結構、時數、先修關係
- [x] `data/glossary.js` — 242 條術語(由 `10-glossary.md` 轉出)
- [x] 其餘資料檔的 schema 定義與空殼

**規劃時沒想到、實作後補上的**
- [x] `app.js` 腳本載入器 —— 術語表與 lab 共用模組按需載入(見 `06` 三層載入策略)
- [x] `home.js` 進度環
- [x] 佔位頁產生器 —— 24 章 + 5 個專區,讓導覽在 P0 就完整走得通
- [x] 術語表頁面(資料已備妥,直接做成可用頁)
- [x] `tools/gen-glossary.mjs` —— 由 `10-glossary.md` 產生術語資料,維持單一來源
- [x] `tools/check-model.mjs` —— 38 項物理驗收,確保程式數值與課文一致
- [x] `tools/check-links.mjs` —— 死鏈檢查
- [x] `tools/smoke.mjs` —— 62 項瀏覽器驗收(Playwright)
- [x] `tools/check-all.mjs` —— 完整品質門
- [x] `tools/serve.mjs` —— 本機伺服器,送出與正式部署相同的嚴格 CSP

**驗收**
- [x] 首頁與一個示範章節頁可正常瀏覽
- [x] 深淺主題切換無閃爍,Canvas 示範元件正確重繪
- [x] 進度追蹤可記錄與匯出
- [x] 無障礙檢核清單(`07`)全數通過
- [x] 關鍵路徑 < 120 KB、DOMContentLoaded < 300 ms
- [x] `prefers-reduced-motion` 下不啟動動畫迴圈,內容仍完整
- [x] 行動版 375px 不橫向捲動、抽屜可開關
- [x] 無 CSP 違規、無 inline script/style、無 on* 屬性

### P0 實測結果

| 項目 | 結果 |
|---|---|
| 物理模型驗收 | 38 / 38 |
| 對比度(深淺兩套) | 38 / 38 |
| 瀏覽器煙霧測試 | 62 / 62 |
| 死鏈 | 0(1,603 個連結 / 35 頁) |
| 關鍵路徑 | 103.7 KB / 15 個資源 |
| A01 元件檔 | 12.6 KB(捲到才載入) |
| DOMContentLoaded | ~150 ms |

### P0 過程中修正的規劃錯誤

實作是最好的審閱 —— 以下都是規劃階段沒抓到、被自動檢查逼出來的:

| 錯誤 | 修正 |
|---|---|
| 課綱總數算錯:4 階 × 6 模組寫成 23 模組 | 改為 24 模組;總時數 59.5 → 60 小時 |
| 互動元件數不一致:內文寫 28、規格表列 32 | 統一為 32(A01–A32),複雜度統計連帶重算 |
| `docs/02` §2.4.1 Bohm 通量少一個數量級 | 1.6×10¹⁶ → 1.6×10¹⁵,並補上 CCP/ICP 對照 |
| `docs/07` 色票有 6 項未達自己訂的對比標準 | 依 WCAG 重新求解;`border-strong` 原僅 1.56:1 |
| `docs/06` CSP 漏了 `style-src-attr` | 拆成 `style-src-elem` / `style-src-attr` 並說明理由 |
| `docs/06` 宣稱初始載入 < 50 KB | 實測 194 KB;改為按需載入後 104 KB,門檻改訂 120 KB |
| 工時估算沿用錯誤的元件數 | 344 → 351 人天 |

---

## P1 — L1 初階

### 為什麼 L1 先做

L1 是**工法驗證期**。7 個元件涵蓋了粒子模擬(A01/A03/A04)、SVG 曲線(A05)、複合動畫(A06)、資料驅動(A07)四種型態 —— 把這些跑通,後面 21 個元件就是重複應用。

### 工作項目

- [x] 6 個模組、約 24 個小節的內容撰寫(1.1–1.6 全部完成)
- [x] A01–A07 七個互動元件
- [~] 約 35 張 SVG 示意圖 —— 實作後這個數字大幅高估:內容改用互動元件
  (A01–A07,比靜態圖更能表達動態概念)與表格承載,靜態圖只剩需要精確
  剖面幾何的地方(見 3.3 缺陷圖鑑的 19 張)。L1 沒有這類需求,視為已滿足
- [x] 章末自我檢測 + L1 結業題庫(實際 54 題,docs/08 目標 50–60,已達標)
- [x] 公式手冊條目(實際 10 條,見 `/formulas/`)
- [ ] 三道審閱全部完成(技術/教學/一致性審閱是人工審閱項目,非自動化可驗證)

### P1 完成紀錄

六章全部上線。自動化門檻:物理 47/47、對比度 38/38、煙霧測試 94/94、0 個死連結。

**被自動化檢查抓出來的錯誤**(留給後面幾期參考):

| 來源 | 錯誤 | 修正 |
|---|---|---|
| A03 模擬 | 課文寫 10 mTorr 碰撞「十幾次」 | 實際約 6 次;100 mTorr 約 60、1 Torr 約 600 |
| check-model | 平均自由徑差 6.2 倍 | 截面改用剛球 σ = √2·π·d² |
| check-model | 0-D 模型 n_e = 6×10³⁰ | 能量平衡式裡 `C.e` 乘了兩次 |
| check-model | Paschen 最小值與課文表格對不上 | A、B 由 (pd_min, V_min, γ) 反推,最小值恆等於表格 |
| 文件複核 | Bohm 通量寫 1.6×10¹⁶ | 正確是 1.6×10¹⁵;連帶修正「每表面原子每秒 20 個離子」→ CCP 約 2、ICP 約 230 |
| check-contrast | 6 個色彩 token 低於自訂門檻 | 重算色值,`--pa-border-strong` 從 1.56:1 拉到符合 WCAG 1.4.11 |
| A06 實作 | `plot.line` 的離群門檻寫成 `ys.min * 0.5` | 值域含負數時會切掉合法資料,電位曲線被截斷;改用值域跨距 |
| A06 實作 | 課文寫電子/離子速度比「數百倍」 | √(M/m_e · T_e/T_i) ≈ 2,700 倍 |

**結論:實作本身就是最有效的審閱。** 上面八項有六項是純文件審閱看不出來、
一跑程式就現形的。P2 之後維持同樣做法 —— 每個寫進課文的數值,都要有一支檢查盯著。

### 特別注意

- **A06 鞘層形成動畫是 L1 的關鍵** —— 它是整個電漿製程觀念的核心,值得多花時間打磨
- **A05 Paschen 曲線** 建立 `plot.js` 的對數軸與多曲線比較能力,後面 A12/A13/A26/A28 都會用到
- 1.6 的 recipe 拆解是 L1 的收束點,務必寫得具體

---

## P2 — L2 中階

### 工作項目

- [x] 6 個模組、約 26 個小節(2.1–2.6 全部完成)
- [x] `data/gases.js` 32 種氣體完整資料
- [x] A08–A16 九個互動元件
- [x] 氣體百科頁面(`/gases/`,與 A11 共用同一個元件)
- [~] 約 40 張 SVG 示意圖 —— 同 P1 的說明,由 A08–A16 九個互動元件與
  氣體百科的資料卡承載,沒有發現額外未滿足的靜態圖需求
- [x] 自我檢測 + L2 結業題庫(實際 78 題,docs/08 目標 75–90,已達標)
- [x] 公式手冊條目(實際 11 條)

### P2 完成紀錄

六章 + A08–A16 全部上線。自動化門檻:物理 59/59、氣體 48/48、輪廓引擎 16/16、
對比度 38/38、煙霧測試 127/127、0 個死連結。

新增兩支共用資產,P3 直接繼承:
- `data/gases.js`(32 種氣體)+ `tools/check-gases.mjs`
- `src/js/lab/profile-engine.js` + `tools/check-profile.mjs`

**這一期被實作抓出來的錯誤**(其中三項是規格或物理層面的,不是打字錯):

| 來源 | 錯誤 | 修正 |
|---|---|---|
| A08 統計 | 用「已抽走那批的平均停留時間」估 τ,系統性偏低 7–40 % | 右設限造成,偏差僅以 1/T 衰減;改用 τ̂ = 總曝險時間 ÷ 事件數 |
| profile-engine | SiO₂ 對 Si 選擇比做不出來(1.0) | 離子濺射項壓過氧消耗項;改成氧消耗也需離子先打開反應 |
| A10 | 開口寬度量到第一列膜(t=0 時是實心的)→ 分母為 0 | 改量遮罩層那一列,五種氣體的輪廓判定才分得開 |
| A13 | 鞘層電位寫成「0 到 V_bias 振盪」,雙峰落在座標軸端點抓不到 | 真實鞘層是直流降疊 RF 漣波,雙峰在 V_dc ± ΔE/2 的內部 |
| **A15(規格層面)** | **規格只給兩顆電容,但電漿是強電容性負載,再加電容只會更偏** | **真實匹配盒有固定串聯電感;加上 L = 3.5 µH 才匹配得了** |
| A16 | 蝕刻率 5×10⁷ nm/min 且動 bias 完全不變 | 改為相對預設正規化,離子輔助做成乘積(Coburn–Winters 協同) |
| A16 | 因果鏈變化量永遠顯示「幾乎不變」 | 滑桿連發兩次 onChange 覆蓋基準;加上「值沒變就不記錄」防護 |
| plot(共用) | `tickCount: n` 產生 n+1 格,整數格式會印出重複刻度 | 重複標籤跳過,格線照畫 |

**A15 那一項值得特別記下來**:它不是實作失誤,是<strong>規格書漏了一個元件</strong>。
純文件審閱不會發現,因為「並聯 C_tune + 串聯 C_load」讀起來完全合理 ——
要真的把 Smith 圖算出來,才會看到它怎麼掃都收斂不了。

### 特別注意

- **A16 虛擬機台控制面板是全站最複雜的元件**,建議排在 P2 最後,等 `plasma-model.js` 在 A08–A15 中被反覆驗證後再做
- **A10 F/C 比滑桿** 建立 `profile-engine.js` 的雛形,P3 的 A18/A19/A20/A22/A23 全部繼承
- 氣體資料的**危害分級與相容材質必須查 SDS 核實**,不可憑印象填

---

## P3 — L3 進階

### 工作項目

- [x] 7 個模組、約 30 個小節(約 68,000 字)
- [x] 3.1 蝕刻機制(含 A17、A18)
- [x] 3.2 主要蝕刻應用(含 A19 Bosch)
- [x] 3.4 電漿沉積(含 A22 PEALD、A23 HDP vs PECVD)
- [x] 3.5 濺鍍 PVD 與其他應用(含 A24 磁控 E×B 與 racetrack)
- [x] 3.6 均勻度與腔體工程(含 A25 晶圓分佈熱圖 + 反向練習)
- [x] 3.3 缺陷圖鑑(含 A20 ARDE、A21 診斷器)+ 獨立的 `defects/` 圖鑑頁
- [x] **3.7 封裝電漿清潔與表面處理(含 A33)** —— 規劃完成後增補的模組
- [x] `data/defects.js` 18 種缺陷 + 成因矩陣(實際 19 條,含增補的 first-wafer)
- [x] A17–A25 九個互動元件
- [x] 缺陷圖鑑頁面 + 診斷器(A21),含 19 張 SVG 剖面縮圖(`data/defect-svg.js`)
- [~] 約 45 張 SVG 剖面示意圖 —— 圖鑑本身的 19 張已完成且是這批裡教學
  價值最高的部分(症狀辨識靠圖,不是靠互動元件);其餘估計數字同 P1/P2
  的說明,大部分被 A17–A25 九個互動元件與資料表吸收
- [x] 自我檢測 + L3 結業題庫(實際 92 題,docs/08 目標 88–105,已達標;
  情境判讀題占比 18%,涵蓋圖形判讀)

### 特別注意

- **A18 輪廓模擬器是全站第二複雜的元件**,且必須能重現八種 profile。建議先做出「垂直 + undercut + taper」三種驗證引擎正確,再擴充其餘五種
- 缺陷圖鑑的 SVG 剖面圖是**教學品質的關鍵** —— 特徵必須明確到學員能一眼分辨 bowing 與 undercut。這部分要找有實際看過 SEM 的人審
- A21 診斷器與 A18 的連動(點成因 → 帶參數進模擬器驗證)是很強的教學設計,不要省略
- **3.7 是規劃完成後增補的**:後段(封裝廠)的電漿與前段差異大到必須單獨講 ——
  有機材料、低溫限制、大面積、幾乎不用氟系,而且**製程窗有上限**。
  它的模型(`package-model.js`)與輪廓引擎完全無關,輸出是接觸角/接著力而非 CD,
  由 `tools/check-package.mjs` 34 項斷言守住,已進品質門。

#### 3.2 / A19 實作紀錄

A19 沿用 profile-engine,只在兩組參數之間交替 —— scallop 不必寫任何規則。
但過程補上兩個引擎參數,都是「用介電質的係數去跑矽」跑出來的問題:

| 問題 | 修法 |
|---|---|
| 沉積步一輪就把溝封死,深度只有 1 列 | `polySputterRel`(預設 1):聚合物濺射係數 0.03 是照介電質校準的,那裡靠 SiO₂ 自身的氧清聚合物;**矽沒有氧**,只剩這一項,Bosch 的清底步需要更高的倍率 |
| 關掉沉積步側壁只鼓 10 %,課文說「立刻被咬爛」 | `chemRel`(預設 1):0.35 是照氟碳化學校準的,而 **SF₆ 對 Si 又快又等向**,那正是 Bosch 要分步的理由 |

**規格層面補上一項**:docs/03 寫「循環時間短 → scallop 小,但產率低」。
第一版模型跑出來是**反過來的** —— 短循環 scallop 小、每秒深度還更高,
那 Bosch 就沒有理由用長循環。少的是**每循環固定的切換開銷**
(換氣、抽殘氣、電漿穩定,模型取每次 0.6 s)。加上之後短循環的開銷佔比
19 % vs 長循環 6 %,課文的結論才成立。**讓 trade-off 成立的是機台,不是化學。**

`tools/check-bosch.mjs` 13 項已進品質門,兩個觀察點都被斷言。

#### A18 實作紀錄(進行中)

「必須能重現八種 profile」比預期難得多,而難的地方不是畫圖,是**引擎的物理不夠對**。
第一次實作跑出來只有三種成立。追下去修掉了五個實質錯誤(細節見 `docs/05` 的 A18 實作備註):

| 問題 | 修法 |
|---|---|
| 八種輪廓停在不同深度,三段寬度無法比較 | 改成蝕到終點再過蝕刻 10 % 才量 |
| ARDE 幾乎不存在(落差 < 2 %) | 離子輔助項補上**本地**自由基覆蓋率(Langmuir);落差變 39/51/67 |
| 側壁的聚合物被無離子地清掉 | 氧消耗聚合物加上離子閘 —— 這才符合 3.1.4 |
| 遮罩中途被濺鍍光,低鈍化預設全變 faceting | 遮罩 chem 0.06→0.01、ionY 0.35→0.18、厚度 0.16→0.20 |
| faceting 與 undercut 用三段寬度分不開 | 新增「遮罩開口(倍)」讀數作為判別 |

**規格本身也錯了一處**:原本把 bowing 歸因於「鈍化不足」,但低鈍化會讓頂部先被咬開,
量出來是 undercut。bowing 要成立,頂部必須被沉積保護、中段靠離子反射打開 ——
所以 bowing 的鈍化必須**偏高**。這種錯誤純看文件永遠找不到。

配套產出:
- `src/js/lab/profile-shapes.js` —— 物理與 UI 分離,讓驗收條件可以被自動斷言
- `tools/check-shapes.mjs` —— 驗收測試,跑的是與瀏覽器同一份程式碼(尚未達成,故不進品質門)
- `tools/shapes-search.mjs` / `shapes-sweep.mjs` —— 在受限參數空間搜尋預設(開發工具,不進門)

#### A18 目前狀態:五種成立,三種未收斂

`npm run check:shapes` **目前 21 / 32**,而且它**刻意不進品質門** ——
它是一份還沒達成的驗收條件,不是回歸測試。把它放進門只會逼人去改測試。

| profile | 狀態 |
|---|---|
| 垂直 | ✅ |
| Taper | ✅ 底/頂 0.54(離子角度發散大 + 中等鈍化) |
| Footing | ✅ 界面處 0.23(鈍化偏高 + 離子方向性好) |
| Faceting | ✅ 遮罩開口 1.77×,而且是**真的削出斜面**(見下) |
| Etch stop | ✅ |
| ARDE(多溝槽) | ✅ 落差約 45 %,由覆蓋率自然產生 |
| Undercut | ✗ SiO₂ 對純化學蝕刻本來就抗 —— 見下方診斷 |
| Bowing | ✗ 側壁鈍化飽和,沒有「部分保護」的中間態 |
| Microtrench | ✗ 同上 |

#### 這一輪補上的物理(四項,全部是實作才浮現的)

1. **濺鍍產額的角度依賴 Y(θ)** —— 課文 3.1.6 教了整整一節,引擎裡卻沒有。
   用 Yamamura 型 `(1/cosθ)^3 · exp(−1.5(1/cosθ−1))`,峰值在 60°、約 1.8 倍。
   加上之後 faceting **才是真的削出斜面**,而不是靠「遮罩整體變薄」冒充。
2. **物理濺射與離子輔助化學蝕刻的角度依賴必須分開。** 第一版把完整的 Y(θ)
   也套在化學項上,結果任何斜面都被清得特別快,溝變成**完美矩形** ——
   比真實製程乾淨太多,taper 與 footing 直接被抹平。
   化學項改用 `1 + 0.35(Y−1)`(它由自由基覆蓋率主導,不是由反衝原子噴不噴出來
   決定),taper 與 footing 立刻成立。**這一項讓成立數從 4 變成 5。**
3. **再沉積(redeposition)** —— 從溝底打下來的材料會落在附近表面上重新黏住,
   落點集中在溝的下半段。這是 taper 與 footing 的機制之一。
4. **鏡面反射的通量門檻寫得太高**(`inc.f <= 0.02` 就跳過)。側壁的直射通量
   本來就小,但它是**掠射**進來的、反射比例最高 —— 正是要抓的那一份。改為 0.002。

#### 三種未收斂的**具體診斷**(供接手者直接動手)

- **Undercut**:A18 的膜是 SiO₂,而 **F 自由基蝕 SiO₂ 需要離子先破 Si–O**
  (這正是 3.1.4 的化學選擇性),所以純化學側蝕本來就很小 —— 模型是對的,
  是**題目選錯材料**。undercut 是 poly-Si / Al 蝕刻的缺陷,不是介電質蝕刻的。
  → **修法:讓 A18 的目標膜可選(Si / SiO₂ / SiN)**,在 Si 上示範 undercut。
  這同時讓 A18 能示範選擇比,教學價值比硬湊 undercut 高。
- **Bowing / Microtrench**:兩者都需要側壁處於**部分保護**的狀態,
  但目前 `poly` 在側壁會累積到 4×polyCrit,`block = 1 − poly/polyCrit` 直接飽和成 0
  —— 模型只有「完全保護」與「完全清開」兩態,沒有中間的梯度,
  所以側壁不可能出現「中段比上下段多被咬一點」。
  → **修法:讓側壁的聚合物有上限(例如 clamp 在 1.2×polyCrit)**,
  使 block 落在 0…1 之間可變;或讓聚合物本身會被斜射離子緩慢濺射掉。
  這一改會動到 etch-stop 的邊界,要一併重新確認 F/C 分水嶺。

#### Undercut:目標膜已可選,但**判定**卡住(第三條記錄)

`profile-shapes.js` 已支援 `state.film`(oxide / silicon / nitride,見 `FILMS` 與 `layersOf`),
下層自動配對(矽膜配氧化層下層 = poly gate;氧化膜配矽下層 = 接觸孔)。
同時補了 `paramsFor(state)`,讓 `state.engine` 可以覆寫引擎參數 ——
從外面覆寫 `S.stepParams` 沒有效果的那個坑就此拔掉。

**在矽膜上側蝕確實出現了**(這是對的:silicon 的 `chem` 是 1.0、oxide 只有 0.25):
undercut 預設在矽膜上量到頂部 **257 %**、中段 114 %、底部 43 %。氧化膜則是 100/100/100。
所以「undercut 要在 Si 上示範」這個判斷成立。

**但 `classify()` 把它判成 Faceting**,原因是**判別指標本身有缺陷**:

- `maskMetrics().widen` 量的是**遮罩最下一列**的開口寬。膜被側蝕之後,
  遮罩下方變成懸空的簷,那一列很快就被從下面咬開 → widen 衝到 2.57,
  越過 `> 1.25 → Faceting` 的分支。
  **而 widen 這個指標當初就是為了把 faceting 和 undercut 分開才加的** ——
  它現在對 undercut 也有反應,等於失去了鑑別力。
- 直覺的修法「改量遮罩**最上面**那一列」**也不對**:實測氧化膜的 undercut
  (判定為垂直、widen = 1.00)openTop/base 已經是 **2.86**,
  遮罩肩部 `shoulderRatio` 甚至是 0.00。也就是說遮罩上緣在各種預設下都會被削,
  上緣寬度同樣分不開兩者。
- 掃描 100 組(ion 200–900 × radical 15–95 × passiv 0–28,矽膜)
  **沒有任何一組**同時滿足「頂部 > 125 %」與「widen ≤ 1.25」。

→ **下一步不是再調參數,是重新定義 faceting 的判別**。
可能的方向:比較遮罩**剩餘厚度的橫向分布**(faceting 是肩部薄、遠處厚的斜面),
而不是比較開口寬度 —— 開口寬度同時被兩種缺陷推動,先天分不開。
`fieldLeft` 與 `shoulderRatio` 已經量好了,但目前只在 faceting 分支後面才用到。

#### 試過但**沒有用**的兩條路(別再走一遍)

1. **加強再沉積(redeposition 0.5 → 10)**:完全沒有差別。原因是側壁的聚合物
   已經飽和(`block = 0`),再多疊一層擋不住更多東西 —— 它是 no-op。
2. **聚合物自發損失(polyLoss 0.05 / 0.15 / 0.4)**:動機是讓側壁膜厚有穩態值、
   讓 `block` 出現梯度。實測**不足以讓側壁出現「中段多被咬一點」**,
   而且會把 taper 的製程窗推掉(taper 變成 footing)。引擎保留這個參數並註明,
   但 `profile-shapes.js` 刻意不開。

**要動 bowing / microtrench,必須連 `block` 的飽和一起重新設計**,
單獨加機制沒有用 —— 上面兩次都是這樣失敗的。

> ⚠️ **探測工具的坑**:`S.stepParams = ...` 從外面覆寫**沒有效果**,
> 因為 `step()` 呼叫的是模組內部的 `stepParams`。要試新參數必須直接改
> `profile-shapes.js`。前兩次探測就是踩到這個,得到「參數無效」的假結論。

**這一項仍必須在 3.3 缺陷圖鑑完成前收斂** —— 圖鑑的診斷區分要靠 A18 驗證,
模擬器做不出來的形狀,圖鑑就沒有依據。3.1 的課文已如實標註目前的限制。

---|---|
| 垂直 | ✅ |
| Footing | ✅ |
| Faceting | ✅(靠「遮罩開口變寬」判別) |
| Etch stop | ✅ |
| ARDE(多溝槽) | ✅ 落差約 45 %,由覆蓋率自然產生 |
| Undercut | ✗ 側蝕不足,量出來仍是垂直 |
| Taper | ✗ 高鈍化下先觸發 footing,側壁角度不夠斜 |
| Bowing | ✗ 反射造成的中段鼓出被溝底的碗狀優勢蓋過 |
| Microtrench | ✗ 同上 —— 反射的集中度贏不過中央的自由基優勢 |

**為什麼卡住(供接手者參考)**:這四種的共同瓶頸是**溝底中央永遠比兩側快**,
因為覆蓋率正比於自由基通量,而自由基通量在溝底中央最高。
要讓反射造成的側向/邊緣集中贏過它,可能需要:

1. ~~濺鍍產額的角度依賴還沒進引擎~~ —— **已完成**。`angularYield()`(Yamamura 形式)
   已經進 `profile-engine.js`,由 `angularYield: true` 選擇性開啟,
   shapes 因此從四種變成五種(Faceting 收斂)。
   過程中犯過一個錯值得記住:一開始把完整的 Y(θ) 也乘到**離子輔助化學蝕刻**項上,
   結果輪廓變成完美的矩形,taper 與 footing 全部消失。
   物理濺鍍與離子輔助化學蝕刻的角度依賴強度不同,必須分開
   (`angY` 用完整峰值,`angYchem = 1 + 0.35(angY − 1)`)。
2. 反射的**通量守恆**:目前多個來源的反射會累加到同一格,沒有上限。
3. 幾何解析度:溝寬只有 13–22 格,「兩側深、中間淺」需要更細的格點才量得穩。

**這一項必須在 3.3 缺陷圖鑑完成前收斂到八種全綠** —— 圖鑑的診斷區分要靠 A18 驗證,
模擬器做不出來的形狀,圖鑑就沒有依據。3.1 的課文已經如實標註了這個限制。

---

### 3.4 電漿沉積完成紀錄

`deposit-model.js` 是獨立於 `profile-engine.js` 的一支(蝕刻只會移除,沉積要往真空側長),
骨架同樣只有三條規則:**抵達**(黏著係數決定通量分布)、**長厚**、**濺掉**(只有 HDP)。
四種製程只是這三條的不同參數,void / cusping / 同形 / 由下往上填滿都沒有被單獨寫成規則。
由 `tools/check-deposit.mjs` 的 24 項斷言守住,已進品質門。

#### 建這支模型時抓到的四個 bug(都是量測假象,不是物理)

1. **場區的膜頂到格點上緣** —— 頂部空間只有 21 列,約 130 步後膜就把上方填實,
   `viewFactor` 看不到天空全部歸零,四種製程只剩「被遮住時的殘餘通量」在跑。
   結果**看起來很漂亮**(全部填滿、零空洞),而且濺鍍項變成完全無效
   —— 開與關的 void 數一格不差。
   → 格點尺寸改由「要沉積多厚」反推,並且只長到 budget 就停。
2. **左右邊界當成固體牆** —— 蝕刻引擎這樣寫沒問題(牆就是牆),沉積不行:
   最外面那一欄每一列都變成「貼著固體的真空格」,兩側自己長出一道膜牆往內爬,
   場區膜厚的探針量到的是那道牆(26 格的預算在 36 步就「達到」)。
   → 改成**鏡射邊界**,物理意義是「一整排相同的溝」。
3. **濺鍍與沉積算在不同格** —— 原本寫成「沉積長真空格、濺鍍削固體膜」兩個獨立迴圈。
   沉積 6 步就把那格膜埋起來,而削掉一格要 11 步,累積的 −0.55 就此凍結,
   永遠碰不到門檻:**埋得比削得快,濺鍍就等於沒寫**(統計到 5965 次「削除」、
   總量 550,實際移除 0 格)。
   → 兩項合併成同一個表面格的**單一淨值**,淨值才是課文講的 dep/sputter ratio。
4. **階梯覆蓋率被量化** —— 量覆蓋率時膜厚只有兩三格,純數格子的比值被量化成
   0.00 / 0.33 / 0.50 這種階梯,同一個製程換個 AR 就跳一大階,看起來像物理其實是解析度。
   → 改成**連續膜厚**(整數格數 + 正在長的那一格的分數,模型本來就記在 `grow` 裡)。

還有兩個**斷言本身寫錯**的地方,是靠實測才發現的:
「TEOS 每一種 AR 都留空洞」(把 TEOS 說得太差了 —— 它填得掉低 AR 的溝,
那正是中等節點用它就夠的原因)、
「HDP 合攏時填充很低、不是靠同形」(修好濺鍍之後 HDP 其實是由下往上填,
合攏時已填 54 %,原本的說法反而變成錯的)。

#### 比較點的選擇:同樣的**淨沉積厚度**

四種製程一律長到「場區膜厚 = 溝寬 × 1.5」才比(同形填滿只需 0.5 倍,所以是三倍餘裕)。
不用「各自跑到停滯」有三個理由:會觸發上面第 1 個 bug、現場比填溝也是在同樣膜厚下比、
HDP 的場區成長率本來就被濺鍍扣掉一截,用同樣淨厚度正好把它一起正規化掉
(實測 HDP 要花 2.0 倍步數才長到同樣淨厚度 —— 這就是 D/S ratio 的代價)。

⚠️ 預算必須是**溝寬的固定倍數**。寫成 `width + 4` 會讓窄溝拿到相對更多的膜
(AR 8 是 1.7 倍溝寬,AR 2 只有 1.2 倍),高 AR 反而佔便宜。

#### A23 的已知不足:HDP 的深寬比極限沒有重現

`docs/05` 的 A23 驗收條件寫著「AR 調到 8 → 連 HDP 也填不進去」,
**這一條沒有達成** —— 模型的 HDP 一路到 AR 10 都填得滿。

真實機制是濺出來的材料在溝口附近堆成一頂帽子而重新夾出 void。
目前的落點規則是「沿深度指數衰減、權重 (1 − 視角因子)」,
已經比原本的「按 (1 − vf) 分給全場」好(那條規則等於把材料無條件送到最深處,
是它讓 HDP 在任何 AR 都填得滿),但仍不足以長出帽子。
→ **要收斂必須把濺出通量的角度分布與直線飛行完整算出來**,
單純調衰減尺度沒有用(已試過 λ = 溝寬)。

3.4 的課文已如實標註這一點,並明講「A23 在極高 AR 的 HDP 結果是樂觀的,
不要拿它當製程窗的依據」。A22 的四項驗收條件則全部達成。

---

### 3.5 濺鍍 PVD 完成紀錄

`magnetron-model.js` 把整章收斂到**一個無因次參數**:Hall 參數 h = ω_c·τ,
等效路徑放大 (1+h²)。磁控的效率、它為什麼喜歡低壓、racetrack 為什麼在那個半徑、
靶材利用率為什麼只有兩三成 —— 全部是這一條的後果,不是四條獨立規則。
由 `tools/check-magnetron.mjs` 的 24 項斷言守住,已進品質門。

**反應式濺鍍的遲滯:第一版完全算不出來。**
只寫靶面收支時,穩態解 Q(θ) 是單調的,升流量與降流量走同一條線,沒有雙值區。
正回饋根本不在靶面,在**氣體收支**,而且要三件事同時成立:

1. 只有裸金屬會 gettering,而**腔壁+基板的面積遠大於靶面**(模型 as/at ≈ 15)
2. 基板上的化合物靠**新到的金屬**埋掉,金屬通量 ∝ 靶面裸露比例
3. 化合物的濺鍍產額只有金屬的 1/6

於是「靶中毒 → 金屬通量崩掉 → 基板也中毒 → 沒人吃氣體 → 分壓暴漲 → 靶更中毒」。
把 as 調回靶面的量級,雙值區立刻消失 —— 這條對照直接寫成斷言,
確保遲滯是機制產生的,不是參數湊出來的。
實測雙值區 15.25–19.75 sccm,兩支的沉積率差 2 倍以上。

⚠️ 掃描發現 as 要到 20 以上雙值區才出現。這不是為了好看而調大:
真實機台的腔壁+基板面積對靶面面積本來就是這個量級。

**A24 的視角選擇**:取「把 racetrack 攤平」的剖面(水平軸 = 繞環的方位方向,
B 垂直畫面)。軸對稱的 r–z 剖面裡 E×B 漂移是**出平面**的,
在那個視角畫擺線是錯的 —— 這一點值得記住,教科書插圖也常畫錯。

---

### 3.6 均勻度完成紀錄

六種 map **不是六個選項**,是八個物理旋鈕疊加的結果;
判定(classify)看量出來的 map,不看使用者選了哪個預設 —— 與 A18 同一個原則。
`tools/check-uniformity.mjs` 21 項斷言,已進品質門。

#### 三個「指標本身有缺陷」的修正(都是實測才發現的)

1. **Edge roll 不能量「外圈 − 肩部」。** 中心快的剖面本來就一路往下掉,
   最外圈當然比肩部低 —— 用這個當判據,**每一張 bull's eye 都被誤判成 edge roll**。
   改成「拿 r = 0.5…0.85 的線性趨勢外推到最外圈,量殘差」之後,
   真的 edge roll 量到 0.54,其餘五種都在 0.05 以內,差了十倍。
2. **環紋不能數「斜率變號次數」。** W 形的斜率也會變號,兩者分不開。
   改成數「二階差分的尖峰」,而且門檻要**相對於背景曲率**(取中位數的 4 倍)——
   用固定門檻時,平緩的 W 形反而數到比銳利的環紋還多。
3. **兩個供應高斯要夠寬、夠重疊。** 原本取 0.55 / 0.45,兩個高斯在中間半徑
   都衰減掉,於是「只有氣體項」也會凹出一個假的 W ——
   那樣「W = 中心快 + 邊緣快疊加」的論述就落空了。放寬到 0.75 / 0.62 之後,
   單獨的氣體項是單調的,W 真的需要電場項才長得出來(斷言直接驗這件事)。

另外修掉一個**課文與數字對不上**的地方:Arrhenius 的 Ea 原本取 0.2 eV
並註解「每 10 °C 約差 8 %」,實際算出來是 22.5 %。改成 0.12 eV(13 %)並更正註解。

#### 順手補上的基本元件

`controls.js` 原本沒有 **button** —— slider/segmented/toggle 都是「設定一個值」,
但 A25 的反向練習是「做一件事」(重新出題)。補了 `C.button`,
用 `.pa-seg` 當容器沿用既有按鈕樣式。

⚠️ 另一個坑:`PA.lab.create` 只會把 setup/reset/tick/draw 搬到 api 上,
**額外的物件方法拿不到**(第一版把調色函式寫成 `api.heatColor`,執行時直接 TypeError,
瀏覽器煙霧測試抓到)。要共用的工具函式放 closure。

---

### 3.3 缺陷圖鑑完成紀錄

**P3 到此全部完成**(3.1–3.7 七章)。

`arde-model.js` 把 ARDE 拆成四個可以**個別關掉**的傳輸限制,
貢獻度分得很開(Knudsen 12.6 / 充電 8.8 / 遮蔽 3.7 / 產物 1.5 個百分點),
四項全關時 ARDE 歸零。`tools/check-arde.mjs` 18 項斷言,已進品質門。

#### 建模時抓到的三個錯

1. **不能用固定時間比 ARDE。** 速率寫成 1/(1+k·AR) 時,積分的漸近解是
   d ∝ √w,**與 k 完全無關** —— 跑得夠久之後不管怎麼調旋鈕,ARDE 都收斂到同一個值
   (實測五種預設全部落在 62–63 %,看起來像「所有旋鈕都沒用」)。
   改成「蝕到最寬的那條達到目標深度」就正常了 —— 這也是現場真正的比較點。
2. **壓力沒有接到離子角度發散上。** 原本 divTan 是獨立參數且預設不動,
   於是「降壓」只改到產物排出那一個小項,ARDE 幾乎沒變 ——
   等於把這一章最常用的旋鈕講廢了。改成 θ ≈ 1.5° + 1.1·√P。
3. **CD 範圍拉太開。** 原本 1.0…0.25,最寬的刻到 AR 3 時最窄的已經 AR 12,
   四個傳輸限制在那裡全部飽和,任何旋鈕都救不動。
   改成 1.0…0.4(AR 3…7.5),才是真實 ARDE 研究的比較尺度。

#### 未達成:反向 ARDE 沒有翻負

模型做得到「高聚合區把 ARDE 壓小」(27 % → 18 %),做不到「窄的反而更深」。

原因:聚合物在 AR→0 時供應最大,所以在它強到足以**翻轉深度排序**之前,
會先把最寬的溝在開口就封死 —— 那是 etch stop,不是反向 ARDE。
試過三種寫法都卡在同一個地方:
  · 除法折扣 `傳輸 ÷ (1+鈍化)` —— 鈍化只能按比例壓小,永遠翻不過傳輸限制
  · 相減競爭 —— 有效,但窄溝的 removal 本來就小,相減對它的相對傷害更大
  · 把聚合物黏著係數與自由基脫鉤(0.85)—— 方向對了,壓小的幅度變明顯,仍未翻負

→ **要收斂必須把「離子驅動的聚合物清除」也寫進去**,而那一項會把深度依賴
反轉回來,所以需要完整的聚合物收支(供應與清除都隨 AR 變),不是這支一維模型的範圍。
check-arde 把「目前仍為正」寫成斷言 —— 哪天做到了那條會失敗,提醒接手的人更新課文。

#### A21 診斷器:判別方法是**組出來**的,不是另外寫一份

`defects.js` 新增 `FLOW_TESTS`(依類別給的診斷流程實驗)與
`methodsFor(id)` / `rank(id, cond)`。判別方法由三個來源組合:
圖鑑的「診斷區分」欄 + 對每個相關缺陷的比對 + 該類別的流程實驗。
**實測每種缺陷最少 4 個判別方法**,超過規格要求的 2 個。

`rank()` 的條件只調整**排序**,不刪選項 —— 這是 docs/05 明列的設計要求:
輸出必須誠實顯示「這個症狀有多個可能成因」。數值面板也刻意顯示
「可能成因 N 個 / 判別方法 M 個」,讓「不要看到第一項就動旋鈕」變成看得見的事。

✅ 症狀選擇已改為 SVG 剖面縮圖(`data/defect-svg.js`,詳見下方「A21 症狀選擇改用
SVG 剖面縮圖」一節)。全站約 75 張靜態 SVG 示意圖中,這 19 張已完成,其餘仍待辦。

---

## P4 — L4 專家 + 測驗中心

### 4.1 電漿診斷完成紀錄

兩支模型:`probe-model.js`(Langmuir I-V)與 `oes-model.js`(光譜 + actinometry),
`tools/check-diagnostics.mjs` 28 項斷言,已進品質門。

**A26 的四個參數全部由曲線算出來**(findVf / fitTe / findVp / neFromIsat),
不是把設定值抄回來顯示 —— 所以「量錯」是可能的,這正是探針教學的核心。
補償正常時誤差 < 0.1–2.5 %,四種氣體(Ar/O₂/CF₄/Cl₂)都準,
因為離子質量真的進了 Bohm 速度。

#### A26 未達成的驗收條件(**後來做到了**,見下方「四項未達成的收斂」)

docs/05 寫「無 RF 補償時 T_e 高估至少 2 倍」。**本模型做不到,而且是可以證明的:**

    ⟨exp((V − V_p − V_rf·f(t))/T_e)⟩_t = exp((V − V_p)/T_e) · ⟨exp(−V_rf·f/T_e)⟩

後面那一項**與 V 無關**,所以任何波形(不只正弦)的 RF 調變在指數區
都只是把曲線整條平移,**斜率完全不變** → T_e 救得回來。
實測 V_rf 從 0 掃到 45 V,T_e 誤差始終在 2 % 以內;
改用貼著轉折的擬合窗也只高估 5–13 %,離 2 倍差很遠。

要讓 T_e 真的被高估,需要模型裡沒有的物理:探針雜散電容分壓、
非 Maxwellian EEDF、探針座面積效應。

**但模型確實抓到一個同樣嚴重的錯誤**:V_rf = 30 V 時
**V_p 從真值 20 V 被量成 −10 V**(差 30 V),V_f 同步平移。
而離子能量 ≈ V_p − V_wafer,所以下游估計全錯。
check 把「T_e 目前沒有被高估」寫成斷言 —— 哪天補上缺的物理,那條會失敗提醒更新課文。

#### A27:actinometry 兩個價值都驗到了

功率 500→1200 W:絕對強度變 55 %、比值只變 2.4 %(差 23 倍)。
視窗透光率 1.0→0.3:絕對變 70 %、**比值完全不變**(視窗透光率同乘在分子分母,精確抵消)。
第二項在現場比第一項更常救人 —— 觀測窗積膜是必然發生的。

另外驗了**失效條件**:改用 Si(5.1 eV)當內標比 F(14.5 eV),
比值的敏感度變成正確內標的三倍以上 —— 直接驗證 4.1.4 的第二個前提。

⚠️ 建模時發現的一件事:強度只寫成 `濃度 × n_e × exp(−E_th/T_e)` 是不夠的。
低閾值物種會壓倒一切,實測 SiO₂ 蝕刻最亮的變成 C₂(2.5 eV)與 Si,
而課文說的主訊號是 CO 與 F。加上**譜線本身的相對強度**(截面與躍遷機率,
真實譜線本來就差好幾個數量級)之後,各製程最強的線才和 4.1.3 對得上。


### 4.2 終點偵測與 APC 完成紀錄

A28 的兩條驗收條件都達成,`tools/check-endpoint.mjs` 35 項斷言全綠。

模型的核心只有一句話:**OES 訊號 ∝ 開口率,而偵測器雜訊是絕對值**。
其餘全部是這句話的後果 —— SNR ∝ 開口率,開口率掉兩個數量級 SNR 就掉兩個數量級。
干涉訊號的計算式裡沒有開口率這一項,所以它的終點時間在四個數量級的開口率上
**逐位元相同**(101.1905 s),這一條可以用等號斷言而不是容差。

#### 「不可靠」必須用統計驗,單次結果會騙人

第一版的探針用單次 `detect()`,結果誤差跨好幾個數量級的開口率都給同一個離散值
(−0.7 % → −53.6 %),看起來像模型壞掉。實際上「低 SNR 下不可靠」本來就是
**統計陳述**:同一組參數跑 24–40 顆種子,看中位誤差與**失敗率**(誤差 > 10 %)
才驗得準。加上 `reliability()` / `reliabilityInterference()` 之後,
OES 失敗率隨開口率單調上升 0 → 18 → 70 → 88 → 90 %,而干涉全程 0 %。

這件事直接寫進課文與 A28 面板:元件同時顯示「這一次的誤差」與「24 次的失敗率」,
並提供「重新取樣」按鈕 —— 對應現場那個真實的誤判:
**工程師拿一片切片正常的晶圓,結論「終點沒問題」**。

#### 實作逼出來的三個修正

1. **原規格的四個演算法選項有三個是同一件事。** 「移動平均 / 一階微分 / 歸一化」
   實測跑出**位元完全相同**的結果:微分法本來就要先平滑,而歸一化只是除以常數、
   argmax 對縮放免疫。歸一化救的是跨片視窗霧化漂移,不是單片的終點時間。
2. **平滑必須是「拖尾」才有延遲代價。** 置中平滑 31 點的有號偏差只有 −0.05 %,
   因為對稱平滑不移動對稱轉折的反曲點。真機控制器看不到未來的取樣點,
   改成拖尾(因果)之後:不平滑 +0.02 %、9 點 +1.25 %、31 點 **+4.64 %**。
3. **誤差最小的判準不能拿來停機。** 「平台間 50 % 門檻」在 0.3 % 開口率時誤差 0.7 %
   (微分法已經 56 %),但它要用到蝕刻**結束後**的平台,是非因果的。
   模型用 `causal` 旗標標記,課文明確區分即時法與事後法 ——
   這也解釋了為什麼事後重新分析的終點時間會和機台當下報的不一樣。

#### 干涉法自己的失效邊界(與開口率無關)

- **條紋預算 < 1 個週期**(SiO₂ @ 633 nm 就是 216.8 nm)時直接回報
  `usable: false`,不硬給數字。膜厚 150 nm 只有 0.69 個條紋。
- **固有時間解析度 = λ/(2n) ÷ 蝕刻率**,SiO₂ @ 5 nm·s⁻¹ 是 43 秒。
  振幅塌陷是一段斜坡而非一瞬間,誤差大小隨「膜厚 ÷ 週期 的小數部分」起伏。
  這也是為什麼干涉的失敗判準用「一個條紋週期」而不是和 OES 一樣的 10 % ——
  拿 10 % 去要求它是在罰它做不到的事。

實作時另一個被實測推翻的直覺:第一版把時間戳掛在振幅窗的**起點**
(理由是「這個窗往前看」),結果系統性早報半個窗(−22.6 %);
掛終點則系統性晚報。掛中點才是無偏的。

### 4.3 電漿誘發損傷完成紀錄

A29 的第一條驗收條件達成;**第二條被實作推翻**,`tools/check-charging.mjs` 31 項斷言全綠。

模型的骨幹是一條算得出來的因果鏈:電子等向、離子垂直 → 幾何穿透率 1/(1+AR²)
→ 電位天花板 V = −T_e·ln(有效電子收集係數) → 沿導線灌進閘極 → 注入電荷累積到 Q_bd。

#### ⚠️ 原規格的「天線比與閘極電位近似線性」不成立 —— 線性掛錯了對象

實測 log-log 斜率(天線比 10 → 3000):**電位 0.078、注入電荷 0.979、到崩潰 −0.979**。
天線比 100 → 1000:電位只多 19 %,劑量剛好 ×9.5。

物理上必然如此:電位由天花板(T_e 與深寬比)決定,而氧化層導通對電壓是指數的;
天線比改的是單位閘極面積要吞多少電流 —— 那是**劑量**,不是電位。
這正是設計規則寫成面積比上限的理由,也直接推翻現場那句
「電位只多了 10 %,應該還好吧」。課文與元件因此同時給兩個讀數,
並把原規格的錯誤釘成反向斷言。

#### 沒有氧化層漏電,天線比在物理上是無效的旋鈕

第一版只放 FN 穿隧,天線比從 10 掃到 3000,電位與劑量**一位數都沒變**。
不是數值問題,是結構問題:浮動天線的電位被電漿自己釘住(電漿導納 ∝ 天線面積),
而驅動也 ∝ 天線面積,兩邊完全對消。天線比只能透過

    V_ox = V_天花板 × G_電漿 / (G_電漿 + G_氧化層)

這個**分壓器**生效,而 FN 式在直接穿隧區低估好幾個數量級,等於把 G_氧化層 設成 0,
分壓器退化。改用擬合公開閘極漏電 t_ox 標度的經驗式(1 V 下每 nm 約四個數量級)
之後,機制才成立。**這一項不是細節,是天線效應本身。**

#### 脈衝救得了磨耗失效,救不了硬崩潰

實測三種 duty 的尖峰電位**完全相同**(4.450 V),只有時間平均與劑量下降,
而劑量幾乎剛好等於 duty(20 % → 0.18)。所以要分兩種失效講:
硬崩潰是場強判準(瞬間),磨耗失效是電荷劑量判準(累積)。
原規格沒有這個區分,但它決定了對策選得對不對 ——
現場「脈衝之後崩潰率沒降」多半就是把硬崩潰當成磨耗在治。

#### Q_bd 必須隨 t_ox 變化,否則結論與現場相反

取常數會得到「1.5 nm 的核心元件比 10 nm 的 I/O 元件更容易被天線打死」。
加上「每薄 0.5 nm 約一個數量級」的公開趨勢後,模型跑出正確且反直覺的結果:
**最脆弱的是 4–5 nm 的中間厚度**(硬崩潰只發生在這裡),兩頭都有保護 ——
薄的靠自己的漏電把電位壓在 0.6 V,厚的把天花板電位除以厚度之後場強反而安全。

對策劑量比較(t_ox 3 nm、天線比 300、深寬比 5、全遮蔽):
天線二極體 ×1.2e−6、遮蔽比例降到 20 % ×1.5e−6、脈衝 duty 20 % ×0.18、低 T_e(1.5 eV) ×0.35。

### 4.4 先進技術完成紀錄

A30 與 A31 的四條驗收條件全部達成,`tools/check-advanced.mjs` 39 項斷言全綠。

#### A30:「窗」必須定義對,教科書的窗驗不過

照兩個閾值取窗(18–55 eV)掃 EPC,變異 **239 %**。不是模型壞掉 ——
**閾值只說移除「開始」,沒說移除「來得及完成」**。20 eV 時 Y_mod 只有 0.067,
移除步的離子劑量清不完改質層,自限制沒發生。
可用窗的下緣是「t_clear ≤ 移除步時間」那一點,**隨移除步時間移動**:

| 移除步時間 | 可用窗 | 窗寬 | 窗內 EPC 變異 |
|---|---|---|---|
| 0.3 s | 46.5–55.0 eV | 8.5 eV | 1.65 % |
| 1.0 s | 25.5–55.0 eV | 29.5 eV | **0.00 %** |
| 3.0 s | 20.5–55.0 eV | 34.5 eV | 0.00 % |

上緣是材料常數(原始材料濺鍍閾值),下緣是配方參數。
現場的意思:**窗太窄時先加長移除步,而不是先改能量。**

協同度窗內 99.6 %(β = 0),能量拉到 120 eV 崩到 41.6 %。EPC = 0.136 nm/循環。
另外補了規格沒列的第四種失效:**purge 不足 → EPC 要好幾個循環才穩且偏高 63 %**,
對應現場「同一批前幾片偏淺、後面越來越深」。

#### A31:兩個時間常數,以及兩次填錯數字造成的假象

τ(T_e) = 6.7 µs、τ(n_e) = 71 µs(比值 10.7),而且是**量出來的** ——
T_e 的衰減不是單一指數,ν_iz 一關掉就換了機制,拿參數當答案會給錯的數字。

模型多做出兩件規格沒要求的事:
1. **脈衝頻率的上下限是跑出來的。** 下限 ≤ 1 kHz(n_e 掉到 CW 的 1 % 以下,
   等於熄了再點);上限 ≥ 50 kHz(孔底回落從 92 % 掉到 11 %)。
   夾出數 kHz 的可用區間,與現場經驗值同量級。
2. **Bias 脈衝治不了充電。** 它把平均離子能量從 92 降到 52 eV,
   但 n_e / T_e / 孔底電位完全不動 —— 天花板由 T_e 決定。這個區分規格沒寫,
   但它決定了「脈衝之後充電沒改善」該往哪裡查。

三個被實測推翻的填值:
- **孔底電容**隨手填 2e-8 F/m² → 電位瞬間跟著 T_e 天花板跑,脈衝反而讓尖峰**變高**
  (點火瞬間 T_e 過衝)。改用「剩餘 100 nm SiO₂」導出的 3.45e-4 F/m² 才對。
- **表面高於電漿電位時仍套幾何遮蔽** → 後輝光每週期只放電 0.2 V,
  模型宣稱「脈衝對孔底充電幾乎沒用」,與現場相反。補上**吸引態收集**後才對。
  該項在 4.3 的所有情形下都是關閉的(表面永遠低於 V_p),check-charging 仍 31/31 ——
  這一點有測試背書,不是宣稱。
- **附著係數** 1.2e-16 → 電負度 α 跑到 1800。改成 8e-18 後 α 落在
  0.67(on)/ 3.9(off),後輝光的離子-離子電漿才是文獻上的樣子。

#### 一個與物理無關、但值得記的前端 bug

A30 第一版在瀏覽器裡直接炸掉:`Cannot read properties of undefined (reading 'label')`。
原因是 **rAF 的第一幀時戳可能早於 `start()` 裡取的 `performance.now()`**,
於是 dt 是負的、動畫相位變成 −1.5e−5,`Math.floor` 給 −1,`STEPS[-1]` 是 undefined。
負的 dt 是瀏覽器的正常行為。修法:正模數 + `Math.max(0, dt)`。
順帶抓到 dt 的單位是**秒**不是毫秒(原本除了 1000,動畫慢一千倍)。

### 4.5 電漿模擬與資料完成紀錄

A32 的三條驗收條件全部達成,`tools/check-global.mjs` 26 項斷言全綠。

**驗收條件不是「近似」成立,是恆等成立。** 功率掃 20 倍,T_e 的變化是
**0.0000 %**、n_e 的線性偏差是 **0.0000 %** —— 因為粒子平衡兩邊的 n_e
直接消掉,方程式裡根本沒有功率。這正是 2.6.3「加功率主要加密度、不加溫度」
的完整證明,而整個 L2 有一半的因果鏈掛在這個結論上。
T_e 是對粒子平衡做二分法求根解出來的,不是查表。

其餘實測:壓力 2 → 200 mTorr,T_e 3.70 → 1.55 eV,E_c 34 → 513 eV;
腔體半徑 0.05 → 0.30 m,T_e 2.44 → 2.16 eV;
Ar 的 E_c 95 eV 最低,Cl₂ 405 eV 最高 —— 分子氣體難維持的定量原因。

#### 把理想結論的破口做成開關,而不是偷偷放進去

規格沒要求,但值得做:打開「氣體加熱」(高功率 → T_g 上升 → 定壓下 n_g 下降
→ n_g·d_eff 變小 → T_e 上升)之後,同一組功率掃描的 T_e 變化從 0.00 %
變成 **27 %**、n_e 線性偏差 21 %。開關預設關閉,課文明講它是理想 0-D
第一件會失準的事。**理想結論要先立得住,才談它什麼時候失準** ——
如果一開始就把加熱放進去,學員永遠看不到那個乾淨的恆等式。

#### 自由基平衡少了母氣體耗盡,會算出「自由基比氣體還多」

第一版跑出 SF₆ 的自由基密度是氣體密度的 **2957 %**。
正確寫法是解耦合平衡 f = k_d·n_e/(L + k_d·n_e),它自然被 f → 1 卡住。
改完之後解離度 O₂ 80 %、Cl₂ 86 %、CF₄ 90 %、SF₆ 97 %,與 ICP 文獻同量級。
(雙原子分子的自由基密度可以超過氣體密度 —— Cl₂ → 2Cl 給 171 % ——
但**解離度**必須 < 100 %,測試釘的是後者。)

### 4.6 量產、良率與安全完成紀錄

最後一章,沒有互動元件。但它有另一種正確性可以守:**交叉引用**。
五個故障情境全部引用了前面章節的結論(4.2.7 的五個 FDC 訊號、
4.3 的天線比與 I/O 元件、3.6 的 first wafer effect、4.4 的鞘層 ∝ T_e),
所以做成 `src/data/faults.js` 資料模組,由 `tools/check-faults.mjs`
驗那些引用真的指得到東西:21 項斷言全綠。

驗的東西包括:五個情境的九個欄位齊備、每條假設都附了怎麼驗、
引用的 FDC 訊號都存在、**五個訊號每一個都至少被一個情境用到**
(沒有列了卻用不上的)、引用的章節都在課綱裡、
每個根因都真的出現在章節頁上、GWP 表遞增且章節頁數字與資料一致。

這是「單一資料來源」原則在**純內容章節**上的應用 ——
數字與交叉引用一樣要被自動化守住,不能只靠人眼。

一個內容上的設計:五個情境的共同結構不是「看到異常訊號」,
而是**某一條線索排除掉了其他可能**(均勻度沒變差 → 排除局部堵塞;
只有邊緣不同 → 排除氣體與功率;FDC 完全正常 → 排除機台)。
章末把這五條並列,因為會看訊號的人很多,會用一條線索排除三種可能的人不多。

---

## 測驗中心完成紀錄(第一批題庫)

`tools/check-quiz.mjs` 31 項斷言全綠。題庫 50 題,**25 章每一章都有題目**;
引擎兩種模式(自我檢測即時解析 / 結業測驗交卷後解析)都經瀏覽器驗證可作答、
可計分、可展開逐選項解析。

### ⚠️ 兩個洩漏,都是測試抓到我自己寫的題目

**第一個:選項長度。** 一開始只寫了個弱代理判準(「正解平均長度不得超過干擾項 1.8 倍」)。
換成真正該問的問題 —— **考生每題挑最長的選項,能不能贏過亂猜?** —— 一測:
**命中率 97.7 %,亂猜是 25 %**。整份題庫當時可以用「挑最長的」考到 98 分,
是壞掉的評量工具。原因很清楚:我的正解帶著說明性的子句,干擾項只是一句短話。

**第二個:粗體。** 把 43 題的正解選項改寫、說明移進 `why` 之後,命中率只降到 81 %。
再查才發現更嚴重的一條:**45 % 的正解選項帶粗體,干擾項只有 1 %** ——
考生根本不必讀題,挑有強調的那個就好。

處置:粗體全部從選項本文剝掉(強調留給解析),再把 20 題過短的干擾項
補寫成完整的誤解陳述。命中率降到 **18 %,低於亂猜的 25 %**,
代表長度已經不帶任何可利用的訊號。兩條判準都留在測試裡。

順帶修正了判準本身的一個瑕疵:**平手要算「還是得猜」,不能算命中**。
四個選項等長時考生得到的資訊是零,把它算成命中會誇大問題、也會逼出無意義的修改。

### 煙霧測試補上測驗中心(9 項)

`PA.curriculum.byId` 那個 bug(是 map 不是函式,自我檢測整個掛不上去)
之所以逃掉,是因為當時煙霧測試只涵蓋章節頁 —— 而**純資料的
`check-quiz.mjs` 驗得了題庫結構,驗不到「在瀏覽器裡真的能作答」**。

補上的 9 項是實際點下去的:兩種模式掛載、章節選單 25 章、
選項可勾選、解析立即展開且含逐選項 why、結業測驗抽得出題、
交卷算得出分數、每題都展開、無 console 錯誤。

補完之後把 bug 重新放回去驗證了一次:三項立刻紅燈(掛載失敗、
章節選單 0 章、0 題 0 選項)—— 這個測試確實攔得住它。
煙霧測試從 196 項增為 **205 項**。

### 題庫規模:誠實記錄的缺口

| 階段 | 出題 | docs/08 目標題庫 | 第一批 | 第二批 | 第三批 |
|---|---|---|---|---|---|
| L1 | 20 | 50–60 | 12 | 18 | **30** |
| L2 | 30 | 75–90 | 12 | 18 | **30** |
| L3 | 35 | 88–105 | 14 | 20 | **35** |
| L4 | 30 | 75–90 | 12 | 18 | **30** |

第二批 24 題是**帶著反洩漏規則寫的**(選項本文只放主張、不加粗體、
干擾項與正解長度相當),所以寫完直接量就是 21.6 %,不必回頭改 ——
把規則變成測試的價值在這裡:它不只抓錯,還改變了下一批的寫法。
題庫 50 → 74 題,命中率 18.0 % → **25.1 %(亂猜正好是 25 %)**。

第三批 51 題(74 → 125)每章補到各 5 題,沿用同一套寫法,
命中率 **19.2 %**,仍在亂猜的 25 % 之下 —— 連續兩批都不必回頭修,
說明把規則寫進測試比寫進文件有效得多。順帶拉開了兩個結構指標:
難度分佈 4 / 71 / 50,L3 / L4 的情境判讀題占比 17 % / 17 %(L1 只有 3 %)。

補完之後才發現測驗頁的警語已經與事實不符:它還寫著「第一批」與
「會把題庫全部出完,無法重抽」。L1 現在是 30 題抽 20,重抽是真的有效的;
而 L3 一度停在 34 題、**比它自己的出題數 35 還少**,所以又補了一題到 3.7。
這是「課文數字必須由實測結果決定」的另一面:**課文裡對自己現況的描述,
同樣會過期**。警語改成逐階段列出實際比值,不再用一句籠統的「尚未補齊」帶過。

實際影響:**L1 有 1.5 倍的重抽空間;L2 / L3 / L4 剛好等於出題數,
重測時只有題序不同、題目完全一樣**,離 docs/08 的 2.5–3 倍還有距離。
測驗頁在開始時直接標示這一點,不假裝可以重抽;`check-quiz.mjs` 也把
「尚未達到 2.5–3 倍」釘成一條斷言,補齊之前不會悄悄變綠。

### 第三批寫出了十對重複題 —— 兩條新斷言

準備寫第四批之前先量了一次同章題目的相似度,結果很難看:
**同章有 11 對題幹相似度超過 0.25,其中 10 對是換句話問同一件事。**

| 題對 | 相似度 | 問題 |
|---|---|---|
| L2-009 / L2-027 | 0.54 | 「E→H 跳變時發生了什麼」vs「最直接會觀察到什麼」 |
| L3-001 / L3-021 | 0.49 | 兩題都在問 Coburn–Winters 的協同 |
| L3-009 / L3-030 | 0.49 | 兩題都在問 racetrack 的 E×B 成因 |
| L3-018 / L3-027 | 0.46 | 兩題都是「PECVD 應力用哪個旋鈕調」 |
| L3-007 / L3-028 | 0.37 | 兩題都是「HDP 為什麼填得滿」 |
| L4-015 / L4-023 | 0.34 | 兩題都是「薄氧化層為什麼更脆弱」 |
| L2-001 / L2-019 | 0.31 | 兩題都是滯留時間的數值計算 |

成因很清楚:**我是「一章補兩題」這樣機械地補的,沒有先看那一章已經考過什麼。**
每一章只有五題的時候,重複兩題等於少掉 40 % 的涵蓋率。

危害有兩層。表面那層是**結業測驗會把整批題庫出完,學員會連續看到兩題一樣的**;
更嚴重的是**題庫的名目題數灌水** —— 125 題實際上只涵蓋 115 個知識點,
「補齊進度」這件事本身變成假的。這比選項長度洩漏更難自己發現,
因為每一題單獨看都沒問題,只有把同章的題目擺在一起才看得出來。

十題全部重寫成不同角度(法拉第屏蔽、純化學蝕刻為何等向、離子化 PVD、
折射率作為組成指標、PEALD 的 purge、UV 損傷與充電損傷的對策差異、
導傳與有效抽速、壓力計選用、腐蝕性氣體管路的水氣、VI probe),
同章最高相似度從 0.54 降到 **0.290**。

然後把規則變成兩條斷言(`check-quiz.mjs` 31 → 33 項):

- **同章任兩題的題幹 bigram Jaccard 相似度 < 0.32**
- **同章不得有兩題的 tags 完全相同**

門檻 0.32 是量出來的不是猜的:留下來的最高是 3.6 的
「背吹的目的」vs「背吹洩漏為什麼是警訊」= 0.290,這兩題確實是不同角度、該留;
而抓到的最低是 L4-015 / L4-023 = 0.341。門檻就卡在這兩個數字中間。
反向測試也做了 —— 把 L2-027 改回原本的問法,那條立刻變紅並印出 `L2-009≈L2-027 (0.54)`。

⚠️ 擴充題庫時這條會擋人。**正確的反應是換一個角度出題,不是調高門檻。**

### 第四批 50 題(125 → 175)

有了重複斷言之後,寫法就改了:**先把每一章已有的 tags 全部列出來,
挑沒被涵蓋的角度,再開始寫。** 這比寫完再靠測試抓省事得多,
50 題寫完一次通過,沒有任何一對觸發 0.32 的門檻。

| 階段 | 出題 | docs/08 目標 | 第三批 | 第四批 |
|---|---|---|---|---|
| L1 | 20 | 50–60 | 30 | **42** |
| L2 | 30 | 75–90 | 30 | **42** |
| L3 | 35 | 88–105 | 35 | **49** |
| L4 | 30 | 75–90 | 30 | **42** |

補進來的多半是前三批繞過的硬題:Ramsauer 極小與截面不是常數、
共振電荷交換為什麼拖出 IEDF 低能尾巴、絕緣基板靠位移電流被蝕刻、
Child–Langmuir 的空間電荷自我阻礙、預鞘層為什麼必須存在、
隨機加熱在低壓下取代歐姆加熱、AlF₃ 不揮發所以鋁只能用氯系、
Thornton 結構區域圖、etch stop 是 ARDE 的極端、low-k 的碳耗盡、
PIC-MCC 的成本為什麼隨密度爆掉、VM 的外插失效。

一個要盯著的指標:**「挑最長的選項」命中率從 18.9 % 升到 22.8 %**。
仍然低於亂猜的 25 %,但餘裕從 6 個百分點縮到 2 個。
原因是這批的正解有不少是「一句話講完的機制」,天然比誤解型的干擾項短不了多少。
下一批要刻意再把干擾項寫長一點,否則這條遲早會被自己推破 ——
**這是一條會隨著題庫成長而逐漸逼近的斷言,不是一次通過就永遠安全的。**

一個前端 bug:`PA.curriculum.byId` 是 map 不是函式,自我檢測整個掛不上去。
瀏覽器驗證抓到的 —— 品質門的煙霧測試目前不涵蓋 `/quiz/`,這一項待補。

### 第五批:上一輪自己預言的地雷,自己踩中了

上一批結尾寫著「下一批要刻意再把干擾項寫長一點,否則這條遲早會被自己推破」。
結果第五批寫完一測,「挑最長選項」命中率不是慢慢逼近 25 %,是直接衝到
**38.5 %**——比亂猜還糟,是全新的洩漏,比第一批的 97.7 % 好不了多少個量級。

原因跟預言的方向完全相反:我沒有把干擾項寫短,而是把**正解寫長了**。
39 題裡有 39 題的正解選項把「為什麼對」的推理直接寫進選項本文
(例如「均勻度不足會讓部分區域過蝕、部分區域殘留,兩者都直接造成電性失效」),
干擾項卻維持第二批以後養成的簡短寫法。兩邊同時往外拉開,比只犯一邊的錯更嚴重。

這正是第一批被抓到的那個洩漏模式,只是換了一種寫法重新發生一次:
**選項是主張,理由在解析**這條規則不是寫一次就永久生效,每一批新題都要重新檢查。
修法是把 39 題正解裡的推理子句剪掉、搬回 `why`(選項本文只留主張),
命中率 38.5 % → **19.0 %**。同一輪也順手抓到兩對重複題:
`L1-007`(Paschen 左支)≈`L1-050`(右支,句型抄了左支的骨架,相似度 0.54)、
`L2-024`≈`L2-048`(同一條解離度公式只換數字,相似度 0.63,是「換數字充題數」的
典型案例)。前者改寫問句骨架、後者整題換成黏著係數存活率計算,兩者都降到門檻以下。

| 階段 | 出題 | docs/08 目標 | 第四批 | 第五批 |
|---|---|---|---|---|
| L1 | 20 | 50–60 | 42 | **54** |
| L2 | 30 | 75–90 | 42 | **54** |
| L3 | 35 | 88–105 | 49 | **63** |
| L4 | 30 | 75–90 | 42 | **54** |

品質門全數通過,煙霧測試 205 / 205,check-quiz 33 / 33。
題庫規模仍未達 docs/08 的 2.5–3 倍,L1 已接近(54/20 = 2.7 倍),L2/L3/L4 還有距離。

### 第六批:同一個洩漏,第三次發生

這一輪刻意跳過已接近上限的 L1,把 50 題全部集中在 L2/L3/L4
(L2 +18、L3 +15、L4 +17),並且在動筆前就提醒自己「正解選項只能放
一句話的主張,推理留給 why」。寫完照舊先跑 `check-quiz.mjs` 再考慮
commit——結果命中率還是衝到 **33.9 %**,又雙叒超過亂猜的 25 %。

檢查後發現:問題不是我不知道規則,是我把規則放錯了地方去檢查。
每一題寫完當下都覺得選項「已經夠短」,但沒有真的跟同題的干擾項
逐字比長度——「聽起來簡短」和「量出來最短」是兩件事,直覺會系統性
地低估自己寫的正解有多長,因為推理的份量在腦中被自動歸類成
「解釋」而不是「選項的一部分」。45 / 48 題(94 %)中招,比例跟
第五批(39/39,100%)幾乎一樣高——**寫的時候有沒有提醒自己不重要,
沒有真的跑數字比對就是沒有防護。**

處置與前兩次相同:45 題正解剪短、推理搬回 `why`,命中率
33.9 % → 15.6 %。順手抓到第三對重複題:`L4-026`(同步脈衝的整體
好處)≈ `L4-064`(同步脈衝的相位差設計),都以「同步脈衝(source 與
bias 同時脈衝)」開頭起手式,相似度 0.33——把 L4-064 的問句骨架
改寫掉之後降到 0.29。

**這一條規則正式記下來,供下一批之前先讀一次:**
寫完當批所有題目後,先跑 `node tools/check-quiz.mjs`,只要「挑最長
選項」命中率沒有印出 ✓,就代表憑感覺判斷選項長度沒有用——一定要
先看數字,再決定要不要修,不要靠寫題目當下的直覺去把關。

| 階段 | 出題 | docs/08 目標 | 第五批 | 第六批 |
|---|---|---|---|---|
| L1 | 20 | 50–60 | 54 | 54(本批未動) |
| L2 | 30 | 75–90 | 54 | **72** |
| L3 | 35 | 88–105 | 63 | **78** |
| L4 | 30 | 75–90 | 54 | **71** |

品質門全數通過,煙霧測試 205 / 205,check-quiz 33 / 33。
L1 已達標(54/20 = 2.7 倍);L2(72/30 = 2.4 倍)、L4(71/30 = 2.37 倍)
接近下限;L3(78/35 = 2.23 倍)還有一批的距離。

### 第七批:題庫規模補齊到 docs/08 目標,斷言由負轉正

跳過已達標的 L1,集中補 L3(缺口最大)+ L2/L4(各補一點就到位):
L3 +14、L2 +6、L4 +6,共 26 題。

這一批吸取了前三批的教訓,寫的時候刻意把正解選項寫成一句話主張、
把推理留給 `why`。合併後跑 `check-quiz.mjs`:**命中率 22.6 %,第一次
不需要事後修正就落在亂猜 25 % 以下**——連續三批(97.7% → 38.5% →
33.9%)的洩漏,到這一批總算沒有重演。同章相似度也一次通過
(最高 0.29),沒有觸發重複題斷言。

四個階段補完後全部落在 docs/08 的 2.5–3 倍區間:

| 階段 | 出題 | docs/08 目標 | 第六批 | 第七批 | 倍數 |
|---|---|---|---|---|---|
| L1 | 20 | 50–60 | 54 | 54(本批未動) | 2.70 |
| L2 | 30 | 75–90 | 72 | **78** | 2.60 |
| L3 | 35 | 88–105 | 78 | **92** | 2.63 |
| L4 | 30 | 75–90 | 71 | **77** | 2.57 |

這是題庫擴充七批以來第一次讓 `check-quiz.mjs` 裡「題庫規模」那條
斷言從**負向**(斷言尚未達標,達標才會變紅)改成**正向**(斷言已
達標,任何一階跌出 2.5–3 倍才會變紅)。測驗頁的警語同步從
「⚠️ 尚未補齊」改成「✅ 已補齊」,並更新 `docs/08-assessment.md`
目標題數的實際對照數字。

**七批題庫擴充的完整教訓,總結記在這裡:**
1. 「選項是主張,理由在解析」這條規則第一批就抓到過(97.7% 命中率),
   但接下來三批各自用不同的寫法重新違反了一次——把理由寫進選項本文
   有無限種偽裝方式,唯一可靠的防線是自動化測試,不是寫題時的自我提醒。
2. 「換句話問同一件事」也發生過三次(第三批 10 對、第五批 1 對、
   第六批 1 對),而且都是同一種模式:兩題共用同一個問句骨架
   (「A 現象發生了什麼」vs「A 現象最直接觀察到什麼」)。
3. 兩條斷言(選項長度、題目相似度)都是**先出包、後補測試**——
   規則不是規劃出來的,是被自己寫壞的題目逼出來的。這正好是整個
   plasma-academy 專案反覆驗證的核心方法論:實作是最有效的審查。

品質門全數通過,煙霧測試 205 / 205,check-quiz 33 / 33。

---

### A21 症狀選擇改用 SVG 剖面縮圖,補上 docs/05 明列的待辦

題庫規模補齊後,回頭處理 docs/05 裡最具體、最早被點名的缺口:
A21(缺陷診斷器)的症狀選擇一直是文字清單,規格書寫得很白:
「症狀以 SVG 剖面縮圖呈現,而非文字 —— 因為工程師是看圖對照的」。

**新增 `src/data/defect-svg.js`**:19 條缺陷各自一份手繪剖面幾何
(`<path>`/`<rect>`/`<line>` 組成的內嵌 SVG 字串)。每個形狀的座標
直接反推自 `defects.js` 已經寫好的 `symptom`/`causes`/`distinguish`
文字,不是憑印象另畫一套 —— 例如 undercut 的最寬處卡在遮罩正下方
(y=42)、bowing 的最寬處在剖面中段,兩者的座標差異就是圖鑑診斷區分
文字裡「量一下最寬處的深度就能分開」那句話的視覺版本;notching 刻意
只讓單側牆面凹進去,對應「有方向性、只咬單側」。

**顏色一律用 `var(--pa-viz-*)` / `var(--pa-surface-sunken)` CSS token**,
不寫死十六進位色碼。這是刻意的架構選擇:Canvas 繪圖需要監聽
`pa:themechange` 才能在深色模式切換時重繪(見 `canvas-theme.js` 的
註解),但內嵌 SVG 用 CSS 變數上色時,DOM 本身會自動跟著主題切換
重新套色,完全不需要 JS——這裡少寫了一整類「忘記處理主題切換」的
潛在 bug。

**視覺 QA 抓到兩個結構驗證抓不到的問題**(用 Playwright + 預裝
Chromium 截圖比對,再次驗證這個專案的核心方法論:實作是最有效的
審查):
1. undercut 與 bowing 初版畫得太像,肉眼幾乎分不出來。
   修法:讓 undercut 的側壁在初始外擴後**維持外擴、不收窄**
   (剖面呈箱型/外張),而 bowing 保持中段鼓出、頭尾收回的桶型 ——
   兩者現在讀起來是完全不同的形狀。
2. etch-stop 那條「本來應該蝕刻到這裡」的虛線在深色背景下幾乎看不見
   (灰色 0.5 透明度)。修法:改用 `--pa-text-subtle` 色、
   透明度提到 0.85、線寬 1.2,截圖確認可辨識。

**`A21` 換上新的 `svgSymptomPicker` 自訂控制項**(取代原本用
`C.segmented()` 的純文字分段控制 —— 該元件只支援 `textContent`,
沒有 HTML/SVG 插入能力,所以另外寫一個 `role="radiogroup"` 的卡片式
選擇器)。`data/defect-svg.js` 加進 A21 的 `PA.lab.define` 相依清單,
CSS 新增 `.pa-defect-grid` / `.pa-defect-pick` / `.pa-defect-thumb`
四個類別。

**新增 `tools/check-defect-svg.mjs`**(10 項斷言,已納入
`npm run check`):19 條全部有專屬幾何(沒有掉進 fallback 標準溝槽)、
viewBox 格式合法、座標落在畫布範圍內、`<title>` 內容與 `defects.js`
的 `zh`+`symptom` 完全一致(避免圖文對不上)、每張圖至少用一個
主題 token 上色且沒有寫死色碼、undercut/bowing 幾何字串確實不同、
不存在的 id 回傳空字串而不是拋錯。

品質門全數通過(含新的 check-defect-svg 步驟),煙霧測試 205 / 205,
A21 掛載檢查(控制項 6 / 數值 4)不受影響。

---

### 公式手冊上線 —— 又一個「內容早就存在,只是沒接出口」的頁面

`/formulas/` 從 P0 就規劃好了(docs/06 估「約 45 條公式」),但一直是
`writeStub` 產生的「尚未實作」佔位頁。跟 A21 的 SVG 縮圖一樣的情況:
真正的公式內容早就在,只是散在 25 章的內文裡沒有集中入口 ——
每一章寫到公式時都在用同一個 `.pa-formula` 元件,從沒缺過。

**新增 `tools/gen-formulas.mjs`**:掃 `src/content/level/` 底下全部
章節 html,用括號配對(不是簡單 regex)找出每個 `.pa-formula` 區塊,
抽出公式本文、名稱、可展開的符號表推導,產生 `src/data/formulas.js`。
跟 `gen-glossary.mjs` 一樣的角色 —— 資料由建置時自動重新產生,
不是手改的第二份真相,`build.mjs` 現在每次建置都會先重跑它。

抽取過程遇到三種寫法並存,一開始只認得第一種,漏了 12/47 條:
1. 正式公式:`.pa-formula__eq` + `.pa-formula__name` + 可展開符號表
2. 關鍵關係式重點框(例如 4.2 的「SNR ∝ 開口率」):`.pa-formula__main` +
   `.pa-formula__note`,沒有配對的 CSS 規則,是後期章節土法接上去的寫法
3. 直接一串反應式(2.3 的 CF₄ 解離鏈),沒有單一「這是哪條公式」的標籤

三種都補上抽取規則後 47/47 全部覆蓋,與原文的 `.pa-formula` 區塊數
完全一致(這個「數量必須對等」的斷言就是 `check-formulas.mjs` 的核心)。

**`build.mjs` 新增 `buildFormulasPage()`**,依 L1–L4 分節列出全部公式,
每條都附「回原文脈絡」連結跳回該公式所在的章節與段落錨點。

⚠️ 建置後 `check-links.mjs` 抓到 4 個死鏈,全部在 `formulas/index.html`
裡:公式推導內文裡原本就有連回其他章節的連結(例如 1.3 頁寫的
`../../3/3-6-uniformity/`),那個相對路徑是算給**原章節頁自己的深度**
用的。把同一段 HTML 原封不動搬到 `/formulas/`(深度不同)時,同一個
相對路徑就指錯地方 —— 這是「複製別處已經寫好的 HTML」這個手法本身
帶來的陷阱,跟 A21 抽 SVG 幾何、抽公式文字用的是同一招,這次踩到了它
的副作用。修法:`gen-formulas.mjs` 新增 `rewriteRelativeLinks()`,
用 `posix.normalize(posix.join(來源頁目錄, href))` 把這類連結解回
根目錄相對路徑,建置時再套用公式手冊頁自己的 `{{base}}` 前綴。
修完 `check-links.mjs` 檢查 1970 個連結、37 頁,無死鏈。

**新增 `tools/check-formulas.mjs`**(10 項斷言,已納入 `npm run check`):
其中第一項比較特別 —— 重新執行一次 `gen-formulas.mjs`,比對輸出與
現存檔案是否完全一致,確保沒有人改了章節內文卻忘記重新產生資料
(這條斷言本身也在部署前的 `npm run check` 裡跑,等於每次發佈都會
自動重新同步一次)。其餘驗證涵蓋率(47 = 47)、id 不重複、url 與
錨點在原文檔案裡真的存在。

品質門全數通過,煙霧測試 205 / 205,死鏈檢查 1970 個連結全部有效。

---

### 互動實驗室 /lab/ 上線 —— 元件本來就是可重掛載的,只差一個目錄頁

跟公式手冊同一個模式:`/lab/`(獨立實驗室入口)從 P0 就規劃了,
一直是「尚未實作」佔位頁。但 33 個互動元件本身早就是**可重複掛載
的獨立單元** —— `PA.lab.define(id, factory, deps)` + `[data-lab]` +
`lifecycle.js` 的 `scan()`/`mount()` 是完全泛用的機制,任何頁面只要
放一個 `<div class="pa-lab" data-lab="A21">` 就會自動掛載,不必為
目錄頁另外寫任何掛載邏輯。這正是它一開始就設計成資料驅動、每個元件
自帶 `deps` 宣告的原因 —— 33 個元件散落在 25 章沒有互相拖累過,
搬到同一頁一樣不會。

**新增 `tools/gen-labs.mjs`**:合併兩份既有的單一來源產生
`src/data/labs.js`——docs/05-animation-spec.md 的 `## A## — 標題 ★`
給正式名稱與複雜度星等,`curriculum.js` 的 `modules[].labs` 給每個
元件屬於哪一章、章節連結是什麼。不再手打第三份清單。

**`build.mjs` 新增 `buildLabsPage()`**:依 L1–L4 分節列出全部 33 個
元件,每個都是完整可互動的 `.pa-lab` 掛載點(不是截圖或說明,是
真的可以拖滑桿的那個元件),星等 ≥ 1 的沿用章節頁既有的
`pa-lab--complex` 樣式與小螢幕提示。元件下方附連回原章節的連結 ——
完整教學敘事與觀察點留在章節裡,目錄頁只提供「捲到哪個就用哪個」
的工具入口,不重複整段教學文字。

**沒有另外寫任何掛載/生命週期程式碼**:`lifecycle.js` 的
`scan()` 用 `IntersectionObserver`(`rootMargin: 200px`)逐一延遲載入
每個元件的腳本,`/lab/` 頁面上 33 個 `[data-lab]` 容器套用的是同一套
機制,不會一次把 33 個元件全部跑起來。Playwright 手動抽測 4 個
(L1 的 A01、有星等的 A05/A21、L4 的 A32)全部正確掛載
(`data-lab-mounted`、有 canvas/svg、控制項 ≥ 4)、零 console 錯誤。

**新增 `tools/check-labs.mjs`**(10 項斷言,已納入 `npm run check`):
與 `check-formulas.mjs` 同一個模式,含「重新執行產生器與現存檔案
一致」的同步檢查,並額外驗證目錄的元件清單同時與課綱、docs/05
的數量完全對等(33 = 33 = 33)。

品質門全數通過,煙霧測試 205 / 205。

---

### 我的進度 /progress/ 上線,順手抓到一個一直沒被呼叫到的 bug

三個長期佔位頁(`glossary/`、`formulas/`、`lab/`)都補完之後,最後一個
是 `/progress/`(章節與測驗總覽、徽章、可列印證書、JSON 匯出匯入)。
跟前三個不同 —— 這個不是「內容早就存在,只差接出口」,是真的要寫新的
互動畫面,因為 `docs/08-assessment.md` §認證 描述的徽章/證書邏輯以前
完全沒有程式碼對應。

**動手前先掃了一遍 `PA.progress`(既有的 `core/progress.js`,`chapter`/
`levelStats`/`exportJSON` 等 API 早就存在,只是沒有頁面用它)**,結果
發現一個一直沒被呼叫到的 bug:`quiz/engine.js` 交卷時呼叫的是
`PA.progress.recordQuiz(...)`,但 `progress.js` 只匯出了 `setQuiz`——
**名字對不上,而且參數簽名也不同**(`setQuiz(key, score, passThreshold)`
vs 呼叫端傳的 `recordQuiz(key, {score, passed})`)。呼叫端有
`if (PA.progress && PA.progress.recordQuiz)` 防呆,所以整段靜默跳過,
不拋錯、不寫入 —— **結業測驗的通過紀錄從專案一開始就沒有真的被存進
localStorage 過**,而品質門一路綠燈,因為沒有任何斷言檢查過
「交卷後 `d.quizzes` 裡真的多了一筆」這件事。修法:把 `progress.js`
的函式改名為 `recordQuiz`,簽名對齊呼叫端已經在傳的 `{score, passed}`
物件,不是反過來改呼叫端 —— 呼叫端是實際在跑的程式碼,更值得信任。

**新增 `badges()`**(docs/08 §認證·徽章的唯一來源):四階個別徽章
(通過對應結業測驗)+「全程完訓」(四階全通過 **且** 全站每一章都完成)。
「全程完訓」的 AND 條件是刻意重點測試的地方:`check-progress.mjs` 專門
驗了「四階都通過測驗但章節都沒完成」這個中間狀態,確認「全程完訓」
真的不會被四階測驗單獨觸發。

**新增 `src/content/progress.html` + `src/js/core/progress-ui.js`**:
章節與結業測驗總覽表、徽章 grid、證書產生器(姓名輸入 + 階段選擇 +
涵蓋模組清單 + docs/08 規定的免責聲明逐字稿)、JSON 匯出/匯入/清除。
`progress-ui.js` 走跟 `js/quiz/engine.js` 一樣的按需載入模式(只有
`/progress/` 那一頁會載),徽章畫面靠既有的 `pa:progresschange` 事件
自動重繪,不必重新整理頁面。

**證書列印是純 CSS,沒有另外拉套件**:點「列印」時在 `<body>` 加一個
`pa-print-cert-only` class,`print.css` 就只留 `.pa-cert`、隱藏進度頁
其餘內容與按鈕本身;`afterprint` 事件觸發時自動移回。

Playwright 手動全流程跑過一次(通過 L1 → 徽章即時反映 → 填名產生
證書 → 列印 class 正確切換 → 補完四階與全部章節 → 全程完訓徽章出現 →
匯出 JSON 檔名正確),零 console 錯誤。`tools/smoke.mjs` 新增「我的
進度」段(5 項),特別驗證 `recordQuiz` 真的接得上瀏覽器裡跑的 quiz
引擎、徽章畫面真的會重繪 —— 這正是前述 bug 曾經逃過的那種檢查空隙,
純資料的 `check-progress.mjs`(vm sandbox)驗不到「兩邊叫同一個名字」
這件事,只有瀏覽器裡跑一次交卷才驗得到。

**第一版直接把徽章/證書 CSS 加進全站共用的 `components.css`,結果撞上
效能預算**:「關鍵路徑 < 120 KB」從 119.0 KB 被推到 123.8 KB —— 徽章/
證書只有 `/progress/` 用得到,卻讓其他 24 章的初始載入一起分攤。
修法比照 JS 已有的按需載入慣例:新增 `css/progress.css`(只有
`/progress/` 會連,靠新增的 `{{extraStyles}}` 模板欄位 + 內容頁 meta
的 `styles` 欄位掛上),把 `badges()` 邏輯也一併從每頁都載的
`core/progress.js` 移到按需載入的 `progress-ui.js`(用
`PA.progress.load()`/`isChapterDone()` 這些既有的公開 API 算,不需要
新的匯出)。改完關鍵路徑回到 119.7 KB,品質門全數通過。

品質門全數通過,煙霧測試 210 / 210(新增 5 項)。

---

## 🏁 25 章全部完成

L1 6 章、L2 6 章、L3 7 章、L4 6 章 —— **全部撰寫完畢**,
33 個互動元件全部實作。這是 P4 剛啟動、L4 才要開始寫的時間點快照;
下方清單當時列的「剩餘工作」到這次整理時(見本文件較前段落的各個
「上線」小節,時間序上都發生在這個標題**之後**,只是先寫進了文件
較前面的位置)已經全部完成。

### 工作項目(全部完成,清單保留作歷史紀錄)

- [x] 6 個模組、約 24 個小節(約 50,000 字)
- [x] OES 譜線資料 —— 併進 `oes-model.js`(不是獨立的 `data/spectra.js`)
- [x] A26–A32 七個互動元件
- [x] 測驗中心:計時、抽題、交卷、解析、重測(301 題,四階全達
  docs/08 的 2.5–3 倍目標)
- [x] 徽章與證書產生(`/progress/`,見「我的進度上線」一節)
- [x] 公式手冊(47 條)、[x] 術語表(242 條)、[x] 進度頁(徽章/證書/
  匯出入)—— 三個專區頁面全部上線,`/lab/` 互動實驗室目錄(33 個
  元件)也一併補上
- [x] 自我檢測 + L4 結業題庫(實際 77 題,docs/08 目標 75–90,已達標)
- [x] 4.6 的五個案例研討撰寫(`data/faults.js`,`check-faults.mjs` 21 項斷言)

### 特別注意

- **A26 Langmuir 探針**與 **A27 OES** 是 L4 的價值核心,兩者都需要真實感的資料。譜線波長必須查 NIST 核實
- 測驗中心要處理的邊界情況比想像多:重新整理、中途離開、時限到期、多分頁
- 證書必須清楚標示「非第三方認證」

---

## 內容產出順序原則

在每一期內,順序固定:

```
1. 資料模組先行     gases.js / defects.js / spectra.js
2. 互動元件的物理模型   先確認計算正確,再做視覺
3. 章節正文          寫作時可即時引用元件與資料
4. SVG 示意圖        正文定稿後再畫,避免重工
5. 題目              最後,確保考的是實際寫進去的內容
```

**最常見的錯誤是先畫圖再寫文**,結果文章寫到一半發現圖不對,重畫。

---

## 工作量估算

| 項目 | 單位工作量 | 數量 | 小計 |
|---|---|---|---|
| 章節正文 | 約 1 天 / 3,000 字 | 180,000 字 | 60 天 |
| 互動元件(低複雜度) | 1 天 | 4 | 4 天 |
| 互動元件(中) | 2.5 天 | 16 | 40 天 |
| 互動元件(高) | 5 天 | 10 | 50 天 |
| 互動元件(極高:A16、A18) | 10 天 | 2 | 20 天 |
| SVG 示意圖 | 0.25 天 / 張 | 140 | 35 天 |
| 資料模組 | — | 6 個 | 12 天 |
| 題目 | 0.15 天 / 題 | 465 | 70 天 |
| P0 骨架與共用庫 | — | — | 25 天 |
| 技術審閱 | — | — | 20 天 |
| 教學試讀與修訂 | — | — | 15 天 |
| **合計** | | | **約 351 人天** |

**單人全職**約 17.5 個月;**三人團隊**(1 內容 + 1 前端 + 1 兼職審閱)約 7–8 個月。

### 分期工作量

| Phase | 人天 | 佔比 |
|---|---|---|
| P0 | 25 | 7% |
| P1 | 55 | 16% |
| P2 | 95 | 28% |
| P3 | 105 | 31% |
| P4 | 64 | 19% |

P2、P3 最重 —— 這兩期是本教材真正的價值所在(氣體選用與缺陷診斷)。

---

## 風險與對策

| 風險 | 影響 | 對策 |
|---|---|---|
| **技術正確性審閱找不到人** | **最高** — 錯誤的教材比沒有教材更糟 | 專案啟動前就先確認審閱人選並排入時程;優先找有實際機台經驗者而非純學界 |
| 互動元件的物理模型過度簡化到誤導 | 高 | 每個元件的驗收條件都包含「與課文數值一致」;模型刻意誇大處必須在畫面標註 |
| A16、A18 複雜度失控 | 高 | 排在各期最後;先做最小可行版本(3 種狀態)再擴充;設定時間上限,超過就砍功能不砍品質 |
| 內容量遠超估計 | 中 | 每章設字數上限(4,000 字),超過就拆;5 分鐘摘要強制存在,確保趕時間的人也有收穫 |
| 術語與數值前後不一致 | 中 | 單一資料來源原則;建置時加自動檢查(數值出現在正文與資料模組時比對) |
| SVG 圖的品質不足以辨識缺陷 | 中 | P3 前先做 3 張樣圖給有 SEM 經驗者確認風格 |
| 深色模式的 Canvas 適配漏做 | 低但煩人 | 納入元件驗收清單;寫一支自動化檢查腳本切換主題後截圖比對 |
| 行動版互動元件不可用 | 低 | P0 就決定重新佈局策略;極複雜元件明確標示建議大螢幕 |

---

## 品質門檻

每期結束前必須通過:

- [ ] 所有新增章節通過三道審閱(技術 / 教學 / 一致性)—— 人工審閱項目,
  自動化測試無法取代,目前尚未安排審閱人選
- [x] 所有新增互動元件通過規格書的驗收條件(4 項已知例外詳見「四項已知
  未達成」,課文與 check 腳本都如實標註,不放寬斷言掩蓋)
- [x] 無障礙檢核清單全數通過(`07`)—— `check-contrast.mjs` 每輪都跑
- [x] `prefers-reduced-motion` 下所有內容可完整理解 —— 煙霧測試涵蓋
- [x] 深淺主題各自截圖檢查 —— 煙霧測試涵蓋(主題切換 + Canvas 重繪)
- [x] 行動版(375 px)與桌機(1440 px)版面正常 —— 煙霧測試涵蓋
- [x] 關鍵路徑 < 120 KB、章節頁 DOMContentLoaded < 300 ms —— 煙霧測試
  每輪都量測(這條偶爾會冷啟動假失敗,重跑即過;120 KB 那條曾在
  「我的進度」上線時被撞到,已修正)
- [x] 全站連結無死鏈(建置時自動檢查)—— `check-links.mjs` 每輪都跑
- [x] 術語表涵蓋所有新出現的術語 —— 煙霧測試逐章驗證 tooltip 查得到定義

以上除了人工審閱那一項,全部已經是**每一輪 `npm run check` 都會自動
重新驗證**的常駐項目,不是一次性做完就結束的清單。

---

### 補上 404 頁 —— 全面盤點後找到的第一個具體缺口

上一輪整理 docs/11 的過期清單時承諾過:下一輪要先盤點,只做「有規格
依據或明顯功能缺口」的事,不要為了硬找事做而亂補內容。盤點 docs/06
`URL 與檔案對應` 一節時發現:文件寫著這個架構的優點之一是「SEO
友善」,但完全沒有自訂 404 頁 —— 打錯字的網址目前會落到 Vercel 的
通用錯誤頁,沒有站內導覽、沒有搜尋入口,是使用者體驗上真實的斷點。

新增 `build404Page()`(`build/build.mjs`),輸出到 `dist/404.html`。
**這一頁踩到一個其他頁面都不會遇到的陷阱**:Vercel 對靜態部署的慣例
是路徑找不到時直接回傳 `404.html` 的內容,但瀏覽器網址列仍停在使用者
打的那個(可能任意深度的)壞路徑上 —— 其他頁面「相對於自己在目錄樹
裡的深度」算 `{{base}}`(例如 `../../`)的寫法在這裡完全不成立。
改用「/」開頭的絕對路徑,不管實際壞掉的網址有多深都對。

**這個決定連帶暴露了 `check-links.mjs` 的一個空隙**:它原本只處理
相對路徑,遇到「/」開頭的連結會用 Node 的 `path.resolve` 直接當成
**檔案系統絕對路徑**解析(等於去查 `/css/base.css` 這種完全不存在的
系統路徑),把每一個新加的絕對連結都判成死鏈。修法:「/」開頭的連結
改成相對於 `dist/` 根目錄解析,不是相對於檔案系統根目錄 —— 這條規則
本來就該存在,只是在此之前全站沒有任何頁面用過絕對路徑連結,所以這個
洞一直沒被踩到。

Playwright 直接載入 `dist/404.html` 驗證:頁首、搜尋按鈕、主題、9 個
站內連結全部正常運作,零 console/network 錯誤。

品質門全數通過,煙霧測試 210 / 210,死鏈檢查 2060 個連結全部有效。

---

### 補上 sitemap.xml 與 robots.txt —— 同一個「SEO 友善」缺口的另一半

補完 404 頁時發現 docs/06 對「SEO 友善」的宣稱還缺一半:有自訂 404 了,
但完全沒有 sitemap.xml 或 robots.txt,搜尋引擎沒有明確的入口清單。

新增 `buildSitemap()`,直接從建置時 `pages` 陣列(每個真的產生內容的
頁面都會 push 進去,`buildPage()`/`writeStub()` 已經在做)生成
`dist/sitemap.xml` 與 `dist/robots.txt` —— 單一資料來源,新增頁面不必
記得手動更新網站地圖,也不會列出還沒真的建置出來的網址。

`tools/check-sitemap.mjs`(7 項斷言)驗證兩份清單逐一對等(sitemap 的
37 個網址與 `dist/` 實際的 37 個頁面完全一致,不多不少)、不含
404.html(那個不該被索引)、robots.txt 指向的 sitemap 網址正確。

品質門全數通過,煙霧測試 210 / 210。

**這一輪的兩個修正(404 頁 + sitemap/robots)都是同一種模式**:先讀
文件裡的既有宣稱(「SEO 友善」),對照實際網站找出宣稱與現實的落差,
而不是憑空發明新功能 —— 跟 A21 的 SVG 縮圖、`/progress/` 的
`recordQuiz` 是同一條方法論。

---

## 四項未達成驗收條件的收斂(專門一輪)

長期掛著的四項未達成:A18 三種輪廓、A20 反向 ARDE、A23 HDP 在 AR > 6 的
填溝極限、A26 無 RF 補償時 T_e 高估。這一輪逐項動手,結果如下 ——
**一項完成、一項找到真正的成因(並證明先前的診斷是錯的)、
兩項維持未達成但診斷變精確**。

### ✅ A26 完成:T_e 高估至少 2 倍

先前判定為「可以證明做不到」,而那個證明**本身沒有錯**:
若落在鞘層上的 RF 振幅是常數,則
`⟨exp((V−V_p−V_rf f)/T_e)⟩ = exp((V−V_p)/T_e)·⟨exp(−V_rf f/T_e)⟩`,
後項與 V 無關 → 只平移、不改斜率。**錯的是那個前提**。

真實探針的振幅**不是常數**:探針尖端對電漿隔著鞘層電容 C_sh、對地隔著
雜散電容 C_stray,RF 由兩者分壓;而 C_sh = ε₀A/s,鞘層厚度 s 由
Child–Langmuir 給 ∝ ((V_p−V)/T_e)^(3/4)(2.4 本來就教過這條)。於是:

- 深負偏壓 → 鞘層厚 → C_sh 小 → 幾乎整個 V_rf 落在鞘層上(調變最大)
- 逼近 V_p → 鞘層薄 → C_sh 大 → RF 被雜散電容分掉(調變變小)

**調變振幅隨偏壓變化**,「與 V 無關」的前提就破了 → 指數區被拉平 →
T_e 真的被高估。實作在 `probe-model.js` 的 `rfAmplitudeAt()`,
新增一個無因次參數 `stray = C_stray/C_sh0`(預設 0.35;設 0 可退回舊行為)。

實測(近轉折擬合窗,V_p 下方 3–9 V,這是現場標準做法):

| V_rf | 量到 T_e | 倍數 |
|---|---|---|
| 0 V | 3.00 eV | 1.00 |
| 20 V | 8.44 eV | 2.81 |
| 30 V | 16.52 eV | **5.51** |
| 45 V | 27.35 eV | 9.12 |

補償正常(V_rf = 0)時同一個窗仍量得準(誤差 0 %),所以高估**不是擬合窗
造成的**。`check-diagnostics.mjs` 31/31,原本那條「【已知限制】T_e 沒有被
高估」的反向斷言已翻成正向斷言。

⚠️ 連帶要更新的事實:分壓讓逼近 V_p 處的 RF 被雜散電容吃掉,所以
**V_p 的推歪反而變小了**(30 V 振幅下由 −30 V 變成 +5 V)。
原本三條圍繞「V_p 被推歪 20 V 以上」的斷言是常數振幅模型的產物,
已改寫成對應新物理的形式。現場的經典症狀本來就是「T_e 高得離譜」,
不是「V_p 差 20 V」—— 新的數字更貼近文獻。

### 🔍 A18:找到真正的成因,而且先前的診斷是錯的

docs/11 原本寫 bowing / microtrench 的瓶頸是「反射造成的邊緣集中
贏不過溝底中央的自由基優勢」,並把修法指向「重新設計 `block` 的飽和」。
**這兩句都不對。**實測 `block` 根本沒有飽和(側壁由上而下是
0 → 0.29 → 0.89 → 0.95 的漂亮梯度),而反射通量在側壁上是 **0.0000**。

真正的成因是 `ionFluxVec()` 裡一行看不出來的錯:
**射線是從固體格本身往上追的**,而側壁格的正上方就是側壁的延續
(還是固體),所以每一條射線在第一步就被擋掉 → 側壁的離子通量
**恆為 0**,不管角度發散開多大、鏡面反射開到多強都一樣。

它還連帶讓 `reflectMap()` 整段失效:那裡要求 `inc.f > 0.002` 才計算反射,
而側壁的 inc.f 是 0 —— 先前為了抓「掠射進來、反射比例最高的那一份」
把門檻從 0.02 降到 0.002,自然完全沒有效果(那次修改的註解還留在原地,
正好說明作者當時相信側壁有「很小但非零」的通量)。

把射線起點改到法線外推一格的真空格之後,**microtrench 立刻長得出來**
(µtr 由 −2 變成 +8),faceting 的遮罩開口也還在(widen 1.62)。

⚠️ 但**預設仍然關閉**(引擎參數 `wallFlux`,與 `polyLoss` 同一種處理:
支援、預設不開、原因寫在程式碼裡)。理由是打開之後 A18 的八組預設
全部要重新校準:側壁一旦會被蝕刻,`reflect` 開到 95–100 的那幾組
(bowing / microtrench)會把 µtr 推過門檻,於是 taper / footing / bowing
全部被判成 Microtrench,成立數反而從 5 掉到 4。**不能為了收斂三種
而弄壞另外三種**,所以這一輪只把成因與開關留下,不動預設。

已經確認**行不通**的一條路(別再走一遍):再乘一個幾何投影 cos θ
去壓掉側壁的量。那會與 `angularYield()` 重複計算 —— Yamamura 的
Y(θ)/Y(0) 本來就是對「單位入射通量」歸一的。實測結果是 widen 全部塌回
1.00、µtr 永遠上不了 3、mid 永遠超不過 top,**八種形狀一個都做不出來**
(掃了 168 組參數,含 vertical / undercut / bowing / microtrench /
footing / faceting 六個受限空間,沒有任何一組成立)。

→ **下一步很明確**:開啟 `wallFlux`,然後重新搜 `defects.js` 的八組
profile 參數(尤其是 reflect),而不是在引擎裡再加係數。

#### 2026-08 更新:bowing / taper 重搜完成,microtrench 免搜即過

按 docs/13-plan-review.md §6 的建議,把上面那句「下一步」做掉了。
`tools/shapes-search.mjs` 加了 `wallFlux: true` 的搜尋維度(命令稿留在
`tools/` 底下供之後 undercut 用):

- **microtrench**:舊參數(`ion 650 / passiv 50 / reflect 100`)完全不用動,
  單純加上 `wallFlux: true` 就從「垂直 ✅」變成「Microtrench(溝底兩側深)」——
  它本來就在正確的參數區間,只是引擎的錯把訊號蓋掉了。
- **bowing**:`reflect` 從 95 降到 55、`ion` 從 550 拉到 750。舊的高 reflect
  在側壁有通量之後會直接把 µtr 推過門檻變成 microtrench;新參數落在
  「中段確實鼓出(mid 130 > top 115 的 1.12 倍)但兩側不深過門檻
  (utr −34)」的窗內。
- **taper**:`radical` 從 50 降到 35、`reflect` 從 15 壓到 4、`ion` 拉到 650。
  wallFlux 開啟後側壁被蝕刻,舊參數的 bot/top 收不到 0.8 以下(側壁被
  蝕得太均勻,判成 Footing 或 Microtrench);新參數讓化學蝕刻的貢獻小
  一點、反射壓到最低,bot/top = 0.54,乾淨落在 taper 判準內。

`check-shapes.mjs` 由 21/32 推進到 **28/32**,與 docs/13 估計的「約 28/32」
吻合。剩下的 4 條紅字全部是既有的 **undercut** 已知缺口(判定、與垂直的
差異量測、對策方向)——遮罩開口在打開 wallFlux 後也一併被撐大,直接被
`classify()` 判成 Faceting(`widen` 超過 1.25),這是幾何本身的限制,
不是參數沒搜到,**留給下一輪單獨處理**(docs/13 §6 原文就是這樣分工的,
沒有改變)。

### ⚠️ A20 反向 ARDE:仍未達成,但診斷從定性變成定量

試過補上「離子驅動的聚合物清除」(`dep_net = max(0, dep − k·ion)`),
動機是:沉積 ∝ reach(ar) 掉得快、清除 ∝ ion(ar) 掉得慢,兩者的深寬比
依賴不同,淨沉積會在寬孔最大 → 寬孔被拖住 → 窄的比較深。

**方向是對的,但量不夠,而且可以算出來為什麼**:在比較點
(最寬的那條蝕到 AR 3、最窄的到 AR 7.5,壓力 3 mTorr、
黏著 0.03、duty 0.5)

- 要讓窄孔贏,寬孔的淨沉積必須 > 0.282 → 需要 `polyStrength > 27.8`
- 但 ar = 0 時 reach = 1,沉積最大;不讓所有溝從第一步就停住
  需要 `polyStrength < 12.5`

**兩個條件沒有交集**,所以在這個 0-D formulation 裡怎麼調都翻不了負。
(把 `ionClear` 拉大可以解掉停滯,但同一個係數會把比較點的寬孔懲罰
一起清掉 —— 自相矛盾。)實測這個改動還會弄壞兩條原本綠的斷言
(「加聚合性氣體會把 ARDE 壓小」由 27→18 % 退化成 27→26.5 %,
以及「相減競爭」那條),所以**已回退**,check-arde 維持 18/18。

→ 要收斂需要的是**空間解析的聚合物收支**(沉積與清除都隨深度變、
而且側壁與孔底分開算),那已經是 `profile-engine.js` 那一類的
二維模型,不是這支 0-D 傳輸模型的範圍。

### ⚠️ A23 HDP 在 AR > 6 的填溝極限:本輪未動

排在最後,本輪的時間用在前三項上,沒有動它。原診斷維持:
需要把濺出通量的角度分佈與直線飛行算出來,只調落點衰減尺度不夠。

---

## 3.7 封裝電漿:術語 tooltip 補標記、Ar+O₂ 氣體、內容修正三則

使用者直接指出網站與說明不一致 + 要求補強封裝應用內容,這一輪做的事:

### 術語 tooltip 覆蓋率:14 % → 48 %

docs/12 掃描時判定「75 條術語從未出現在課文」是缺口,但實測發現更大的
落差是**課文有寫、卻沒有用 `.pa-term` 標記起來** —— tooltip.js 對查不到
定義是靜默失效,沒有標記的文字更是完全不會被摸到,所以這個落差此前沒有
任何東西會叫。標記前全站只有 52 處標記、35 條術語真的掛得出 tooltip
(242 條裡的 14 %)。

補上每章「首次出現」的術語標記(docs/09 明文規則)後到 115 條(48 %)。
新增 `tools/check-glossary-coverage.mjs`(6 項斷言,棘輪式:覆蓋率下限
只能往上調、`.pa-term` 標記錯字視為失敗、「課文完全沒提到」的術語數
上限只能往下調),已納入 `npm run check`。

副作用:標記補完後章節頁關鍵路徑正好頂到 120.0 KB(上限 < 120)。
查出來是 `home.js`(只有首頁/階層頁的進度環用得到)被模板每頁都載,
25 個章節頁白背 2.3 KB。改成與 glossary/lab/quiz 同一套按需載入,
回到 118.6 KB,並補一條 smoke 斷言守住「章節頁不載入 home.js」。

### 3.7 補上 Ar+O₂ 混合氣體(使用者直接指出的缺口)

`package-model.js` 的氣體清單只有 Ar / O₂ / H₂-Ar / N₂ 四支,漏了現場
另一個常見配置。新增 `aro2`,係數的設計原則:

- **act = 0.95**(略低於 O₂ 的 1.0)—— 化學活化終究由 O₂ 主導,稀釋後
  動力學略慢,不宜宣稱比純 O₂ 活化更快。
- **etch = 0.55**(明顯低於 O₂ 的 1.0)—— 這才是真正的效果:Ar 的物理
  轟擊持續清開反應路徑(呼應 3.1 的 Coburn–Winters 離子輔助化學),
  讓 O₂ 不必堆那麼多劑量就能生效,同樣活化程度的損傷少了近半。
- 兩者的比值(act/etch,製程窗寬度的代理指標)是五支氣體裡最高的 ——
  `check-package.mjs` 新增斷言驗證這個排序,而不是只驗證數字存在。

A33 實測驗證了這個設計:EMC、300 W、90 秒,純 O₂ 已有 27 % 損傷、
接著力從峰值 1.41 掉到 1.11;Ar+O₂ 同條件 0 % 損傷、接著力 1.44,
要到 150 秒後才開始出現明顯損傷 —— 製程窗多出將近一倍時間餘裕。
這些數字已寫入 3.7.2 的課文與新增的自我檢測題、以及一題新的情境題
(`L3-104`)。

`check-package.mjs` 36/36(含 gases.length 4→5 與「只有一支氧化性」
改寫成「兩支氧化性」的必要更新 —— 這不是放寬斷言,是斷言本來描述的
事實隨新增氣體改變了,斷言要跟著改)。

### 課文裡的數字會過期,這次抓到的例子

`content/quiz.html` 的說明文字寫死了「目前共 301 題」與各階段的倍數,
但這一輪之前(L4 情境題、L3 圖形判讀題)已經把題庫推到 325 題,
L3 92→103、L4 77→90,文字沒有跟著更新 —— 使用者直接在網站上看到
數字對不上。已修正為 325 題、L3 2.94 倍、L4 3.00 倍。

這是「課文裡對自己現況的描述同樣會過期」(見前面第五批題庫的教訓)
再發生一次,而且這次是站上唯一一處**手寫的**題庫統計數字 ——
其餘地方都是 `check-quiz.mjs` 動態算出來的。沒有把它也變成動態產生
是因為改動風險與效益不成比例(一句話,而不是整個頁面),但下一次
再改題庫規模時要記得同步這裡。

---

## 3.7 擴充:PCB 除膠渣與回蝕(使用者指定)

### 為什麼放進 3.7 而不是開一個新模組 3.8

原本想開新模組,但撞到一個硬限制:**L3 題庫已經到 105 題,正好是
docs/08 規定的 3.0 倍上限**(出題 35 × 3)。新開一章至少要有自己的題目,
而 check-quiz 只剩 1 題的額度 —— 一個只有一題的章節不像樣。

要開新模組就得同時提高 L3 的出題數(改 docs/08 的測驗規格)、更新
「25 章」的煙霧斷言、首頁統計、課綱……牽動面很大,而**內容本身放在
3.7 是合理的**:兩者都是後段、都是有機材料、都不需要方向性。
所以改成 3.7 的新一節,模組改名為「封裝與 PCB 的電漿清潔與咬蝕」,
時數 2.5 → 3.5 h(總時數 62.5 → 63.5)。

### 內容的核心是一個「結論相反」的對照

3.7.2 花了三個理由說明封裝端幾乎不用氟系;3.7.7 說 PCB 的 desmear
**非加 CF₄ 不可**。這不是矛盾,是**面對的材料不同** ——
FR-4 有玻纖(SiO₂),而 O₂ 電漿對 SiO₂ 完全無效,沒有 F 玻纖就一動不動。

這個對照是整節的教學主軸,也讓 check-package.mjs 原本那條
「氣體清單沒有任何含氟的氣體」必須**縮小範圍**:它守的是**表面活化**
用的 GASES 清單,與 desmear 無關。斷言標籤已改寫講清楚範圍 ——
這不是放寬,是原本的措辭在新內容出現後變得有歧義。

### 數字全部來自模型,不是寫上去的

`package-model.js` 新增 desmear/etchback 段,核心是兩條互相競爭的速率:

```
樹脂 ∝ O 比例 × (1 + 5/3 · CF₄ 比例)   ← 少量 F 反而加速有機物移除
玻纖 ∝ CF₄ 比例                        ← 沒有 F 就是 0(不是慢,是零)
```

係數 5/3 讓樹脂速率的峰值落在 CF₄ = 20 %,與現場常用的 10–25 % 一致;
兩條的交點(齊平)也落在 20 %。**又是一條先升後降的曲線** ——
與 A33 的接著力同一個形狀、同一個教訓,只是這裡加過頭會同時弄壞兩件事
(樹脂變慢 + 玻纖被咬凹)。

check-package.mjs 新增 11 項斷言(36 → 47),守住的是定性結論:
純 O₂ 必然留下玻纖突出、存在最佳 CF₄ 比例、過量時兩件事同時變糟、
desmear 與 etchback 的目標窗不同。速率的絕對值是教學用的典型量級,
課文與斷言都沒有宣稱它是任何特定機台的實測值。

### 又一個「測試自己過期」的例子

煙霧測試寫死「術語表有 242 條」,新增五條 PCB 術語後就變紅 ——
那不是回歸,是測試自己過期。改成從 `glossary.js` 讀,與同一支檔案裡
課綱數字的既有作法一致。這是繼 quiz.html 的「301 題」之後第三次
同類問題,共同的教訓:**凡是「目前有幾條」的數字都不該寫死在
消費端**,要嘛從單一來源讀、要嘛就別寫。

---

## 3.8 PCB 獨立成章(使用者指定)+ A34 + 兩個渲染 bug

使用者上一輪把 PCB 加進 3.7 之後,明確要求「把 PCB 獨立出來一個章節
專門介紹,題目數可以提供因應」。這一輪把它做成完整的一章。

### 章節與模型都切乾淨

- **新增模組 3.8「PCB 電漿除膠渣與咬蝕」**(2.5 h),3.7 改回
  「封裝電漿清潔與表面處理」(3.5 → 3.0 h)。課綱 25 → 26 章。
- **`pcb-model.js` 從 `package-model.js` 搬出來** —— 一章一個模型,
  與其餘章節一致。自由基通量仍共用 `package-model.js` 的
  `radicalFlux()`(那是「功率×壓力 → 通量」的共用關係,不該有第二份),
  所以 A34 的 deps 是 `[package-model, pcb-model]`,順序不能反。
- **`check-pcb.mjs` 21 項**(從 check-package 拆出 13 項 + 新增 8 項),
  已進品質門。新增的斷言裡有兩條是這一輪才想清楚的:
  - 「最佳 CF₄ 是以**齊平度**挑出來的」—— 樹脂最快與孔壁齊平剛好都落在
    20 %,那是參數選擇的結果不是同一件事,斷言要講清楚挑的是哪一個
  - 「**深度達標 ≠ 齊平度達標**」—— CF₄ 5 % 時深度過關、齊平度不過關,
    這正是現場只看深度就收工會漏掉的那一類板子
- **課文表格的每一格都被斷言驗過**(五組 CF₄ 比例的樹脂/玻纖退量 +
  etchback 的 16.0 µm),數字不可能與模型漂移。

### 題庫:L3 出題數 35 → 40

新章節要有題目,而 L3 題庫本來就已經頂到 2.5–3 倍區間的上限(105/35 = 3.0)。
使用者說「題目數可以提供因應」,所以調的是**出題數**而不是放寬倍數斷言:

- L3 出題 35 → 40、時限 60 → 70 min(docs/08 表格同步改)
- 3.8 共 13 題:2 題從 3.7 移過來(chapter/reference 一起改)+ 11 題新寫
- L3 題庫 105 → 116,116/40 = **2.90 倍**,仍在區間內

⚠️ 這裡沒有動「2.5–3 倍」這條斷言本身 —— 那是規格,不是可調的旋鈕。
可調的是出題數,而出題數變了就要連時限一起變,否則每題的作答時間會被壓縮。

### 順手抓到的兩個渲染 bug(都是實際看畫面才發現的)

1. **34 個元件的觀察點強調全部沒有渲染。** `controls.js` 的
   `observations()` 用 `textContent`,所以元件裡寫的 `**強調**`
   一字不差把星號印在畫面上。這是**從專案一開始就存在**的,
   而純資料檢查看不到 —— 只有真的截圖看畫面才會發現。
   改用跳脫後只放行粗體的 `innerHTML`(與 `quiz/engine.js` 的 `mark()`
   同一套規則),並在 smoke 加一條「觀察點不得殘留 `**` 字面」,
   34 個元件逐一驗證。煙霧測試 218 → 252 項。

2. **`fmt()` 會把浮點殘差顯示成科學記號。** A34 在齊平點的玻纖突出是
   兩個相近數字相減,得到 −8.9×10⁻¹⁶,面板上就這樣印出來 ——
   看起來像量到了什麼東西,實際上是 0.0 µm。
   修法:**呼叫端指定了小數位,就代表它宣告了在意的精度**,
   小到那個精度下四捨五入是零的值就印成零。大數那一端維持科學記號
   (n_e 之類仍需要)。

### A34 的一個設計取捨

兩條速率的量級差很多(玻纖 0 → 1.6,樹脂只在 0.30–0.32 之間動),
共用 y 軸時「樹脂先升後降」在圖上**根本看不出來** —— 而那正是本章
要教的第二件事。加了「樹脂速率(放大)」檢視,y 軸只吃樹脂那條的範圍。

⚠️ **刻意不做「各自正規化」**:那會把兩條的交點(齊平點)畫到假的位置,
而交點才是這個元件的主角。要嘛看真實比例,要嘛單看一條 —— 不能兩者混。
順帶為 `plot.js` 補了 `setY()`,讓「同一張圖切換 y 軸範圍」變成通用能力。

---

## 未來可選延伸(不在本規劃範圍)

- Service Worker 離線快取
- LMS 整合(SCORM / xAPI 匯出)
- 英文版
- 影片講解(與互動元件互補)
- 廠內客製章節(各廠自有機台的參數對照)
- 社群功能(問答、案例投稿)
